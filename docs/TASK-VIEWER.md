# Task Viewer - Приложение для управления задачами

Полнофункциональное web-приложение для просмотра и управления задачами проекта, состоящее из backend на Fastify и frontend на React.

## Структура проекта

```
/
├── taskViewerBe/           # Backend приложение (Fastify)
│   ├── src/
│   │   ├── config/         # Конфигурация окружения
│   │   ├── routes/         # API роуты
│   │   ├── services/       # Бизнес-логика
│   │   ├── schemas/        # Zod схемы валидации
│   │   ├── types/          # TypeScript типы
│   │   ├── app.ts
│   │   └── server.ts
│   ├── package.json
│   ├── tsconfig.json
│   ├── .env
│   └── README.md
│
├── taskViewerFe/           # Frontend приложение (React)
│   ├── src/
│   │   ├── components/     # React компоненты
│   │   │   └── ui/        # UI компоненты (shadcn/ui style)
│   │   ├── pages/         # Страницы приложения
│   │   ├── hooks/         # React хуки
│   │   ├── api/           # API клиент
│   │   ├── types/         # TypeScript типы
│   │   ├── lib/           # Утилиты
│   │   ├── App.tsx
│   │   ├── main.tsx
│   │   └── index.css
│   ├── package.json
│   ├── vite.config.ts
│   ├── tailwind.config.js
│   └── README.md
│
└── docs/
    └── tasks/
        ├── tasks-manifest.json  # Источник данных
        └── *.md                 # Файлы задач
```

## Технологический стек

### Backend (taskViewerBe)
- **Runtime**: Node.js >= 20
- **Framework**: Fastify 5.x
- **Language**: TypeScript
- **Validation**: Zod
- **File System**: Node.js fs/promises

### Frontend (taskViewerFe)
- **Framework**: React 18
- **Language**: TypeScript
- **Build Tool**: Vite
- **Styling**: Tailwind CSS
- **UI Components**: Radix UI primitives
- **Markdown**: react-markdown
- **Routing**: React Router v6
- **Date utils**: date-fns

## Быстрый старт

Проект использует **npm workspaces** для централизованного управления зависимостями.

### 1. Установка зависимостей

```bash
# В корне проекта
npm install
```

Эта команда установит зависимости для всех workspaces (be, taskViewerBe, taskViewerFe).

### 2. Запуск Task Viewer

```bash
# Запуск backend (в одном терминале)
npm run viewer:be:dev

# Запуск frontend (в другом терминале)
npm run viewer:fe:dev
```

После запуска:
- Backend будет доступен по адресу: http://localhost:3002
- Frontend будет доступен по адресу: http://localhost:5173

### Доступные скрипты

```bash
npm run viewer:be:dev      # запуск backend в dev режиме с hot-reload
npm run viewer:fe:dev      # запуск frontend в dev режиме
```

## Функциональность

### Список задач (/)
- ✅ Табличное представление всех задач
- ✅ Поиск по названию и ID
- ✅ Фильтрация по статусу
- ✅ Сортировка (по ID, дате создания, статусу)
- ✅ Изменение статуса прямо из таблицы
- ✅ Навигация к детальному просмотру

### Детальный просмотр задачи (/tasks/:id)
- ✅ Полная информация о задаче
- ✅ Красивый рендеринг markdown
- ✅ Навигация по заголовкам (Table of Contents)
- ✅ Подсветка активного заголовка
- ✅ Редактирование метаданных (title, status, branch)
- ✅ Навигация назад к списку

### API Backend

#### GET /api/tasks
Получить список всех задач из `tasks-manifest.json`

#### GET /api/tasks/:id
Получить задачу по ID с содержимым markdown файла

#### PATCH /api/tasks/:id
Обновить метаданные задачи (title, status, dates, branch)

**Особенность**: При обновлении статуса автоматически обновляется как `tasks-manifest.json`, так и соответствующая строка статуса в `.md` файле.

## API Примеры

### Получить все задачи
```bash
curl http://localhost:3002/api/tasks
```

### Получить задачу с содержимым
```bash
curl http://localhost:3002/api/tasks/TASK-004
```

### Обновить статус задачи
```bash
curl -X PATCH http://localhost:3002/api/tasks/TASK-004 \
  -H "Content-Type: application/json" \
  -d '{"status":"in-progress"}'
```

### Обновить метаданные задачи
```bash
curl -X PATCH http://localhost:3002/api/tasks/TASK-004 \
  -H "Content-Type: application/json" \
  -d '{"title":"Новое название","branch":"feature/TASK-004"}'
```

## Конфигурация

### Backend (.env)
```env
NODE_ENV=development
PORT=3002
HOST=localhost
CORS_ORIGIN=http://localhost:5173
TASKS_DIR=../docs/tasks
```

### Frontend

В режиме разработки используется **Vite proxy** для избавления от CORS проблем.

Конфигурация proxy в `vite.config.ts`:
```typescript
server: {
  port: 5173,
  proxy: {
    '/api': {
      target: 'http://localhost:3002',
      changeOrigin: true,
    },
  },
}
```

API запросы используют относительный путь `/api`, который автоматически проксируется на backend:
```typescript
const API_BASE_URL = '/api';  // Проксируется на http://localhost:3002
```

## Production Build

### Backend
```bash
cd taskViewerBe
npm run build
npm start
```

### Frontend
```bash
cd taskViewerFe
npm run build
# Результат в папке dist/
```

## Архитектура

### Backend
- **Fastify** с поддержкой TypeScript и валидацией через Zod
- **Сервисный слой** для работы с файловой системой
- **CORS** настроен для работы с frontend
- **Типобезопасные роуты** с автоматической валидацией

### Frontend
- **React Router** для навигации
- **Custom hooks** для работы с API
- **Компонентный подход** с переиспользуемыми UI элементами
- **Tailwind CSS** для стилизации
- **TypeScript** для типобезопасности

## Статусы задач

- 📋 **Бэклог** (`backlog`) - задача в очереди
- ⏳ **В работе** (`in-progress`) - задача выполняется
- ✅ **Выполнено** (`completed`) - задача завершена
- ❌ **Отменено** (`cancelled`) - задача отменена

## Лицензия

ISC

## Автор

Создано для управления задачами проекта temp-private-2110-new-be-for-fe.
