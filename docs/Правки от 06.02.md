**Универсальный отчет**:
1. В форме "Список файлов" убрать столбец "Статус". (Причина: сейчас по дефолту будут в списке файлов отображены файлы с успешным статусом)

2. Вкладка "Список файлов" отображается и активна только для заданий в финальном статусе task_done (Выполнено)


3. Процесс "Удаление задания". Дополнительная форма подтверждения удаления должна содержать:
Текст: Вы собираетесь удалить {N } заданий
Предупреждение: Будут удалены все связанные элементы с заданием: папка, файлы  (над текстом можно еще подумать)



**6406**
**Создание и переименование пакета**:
1. Наименование пакета должно быть уникальным. Если при создании или переименовании пакета бэкэнд вернет инфу, что такой пакет уже есть, то ошибка на UI 


**Детализация пакета**:
2. Добавить поле "Статус пакета" - показывать текущий статус пакета

3. Добавить функционал "История статусов пакета" (по аналогии с заданиями, функционал под вопросом?!!!!!!!!!!!!!!!!!!!!):
• Наименование пакета
• Статус
• Сотрудник - пользователь, который создал/ изменил пакет 

4. Статусы пакета
<https://sfera.inno.local/knowledge/pages?id=1799080> 

5. Добавить поле "Дата последнего успешного копирования" -  будет заполняться, только когда копирование прошло успешно (статус пакета "Выполнено") 
При неуспешном копировании дата последнего успешного копирования не будет обновляться, будет только меняться текущий статус (статус пакета "Не выполнено")

6. Добавить кнопку "Отменить копирование" 

**Добавление задания  в пакет**:
1. При добавлении задания в пакет показывать "Список пакетов" в табличном формате (по аналогии со "Списком заданий", по аналогии с формой "Удаление задания из пакетов"). 
Добавить сортировку по размеру (чтобы пользователь отсортировал по самому легкому пакету и добавил туда задачу)
На демо в понедельник показать оба варианта (текущая реализация и новый формат для сравнения)
2. Задание можно добавить в 1 или несколько пакетов сразу (массовый выбор)

**Передача в ТФР**
1. В "Списке пакетов" . Если запущен процесс копирования пакетов (статус пакета "Копирование")  нельзя копировать другие пакеты - заблокировать кнопку "Копировать в ТФР"
2. При выборе пакетов и нажатии кнопки "Копировать на ТФР" проверять(на бэке), нет ли пакета с таким наименованием уже на ТФР?
- Если есть, то ошибка на UI
3. ТФР:
- + Сортировка по размеру пакета
