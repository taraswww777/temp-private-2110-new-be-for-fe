# Шаблон: начало работы над задачей (основная репа + подрепо temp-private-2110)

**Подставь переменные:** `TASK_ID`, `VTB_ID`, `BRANCH_NAME` (см. README в этой папке).

---

Начать работу над задачей **{{TASK_ID}}** в новой ветке.

**Контекст:**
- Задача в основной репе: **{{TASK_ID}}** (файл задания: `docs/tasks/{{TASK_ID}}-*.md`).
- В подрепозитории `temp-private-2110/`:
  - Ветка: `{{BRANCH_NAME}}`
  - Префикс коммитов: **{{VTB_ID}}**

**Действия:**
1. Создать ветку для задачи в основной репе (если ещё не создана), например `feature/{{TASK_ID}}-краткое-название` или по имени из файла задания.
2. В подрепо `temp-private-2110/` переключиться на ветку `{{BRANCH_NAME}}` (или создать её при необходимости).
3. Открыть описание задачи в `docs/tasks/` и выполнять требования по шагам.
4. Следовать протоколу управления заданиями (`docs/cursorRules/task-management-protocol.md`).

---

⚠️ **КРИТИЧНО: коммиты и пуш в оба репозитория**

После каждой логически завершённой операции (после каждого изменения кода) необходимо выполнить **коммит и пуш в ОБА репозитория**:

1. **Сначала в подрепо** `temp-private-2110/` (путь: `temp-private-2110/`):
   - коммиты на **английском** языке;
   - префикс: **{{VTB_ID}}:** (например, `VTB-533: Add docs for mocks`).

2. **Затем в основной репо** (корневой каталог проекта):
   - коммиты на **русском** языке;
   - префикс: **{{TASK_ID}}:** (например, `TASK-032: Обновление подмодуля после документации моков`).

Невыполнение этого требования может привести к потере изменений и проблемам с синхронизацией между репозиториями.

---

Выполнить задачу по шагам из описания {{TASK_ID}}, коммитить и пушить после каждого логически завершённого изменения в оба репозитория.
