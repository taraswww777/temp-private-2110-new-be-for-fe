# TASK-023: Поддержка статуса «planned» в Task Viewer

**Статус**: ✅ Выполнено  
**Ветка**: `feature/temp-private-2110`

---

## Краткое описание

Добавить в Task Viewer (бэкенд и фронтенд) поддержку статуса задачи **«planned»** (запланировано), чтобы задачи в манифесте со статусом `"planned"` корректно проходили валидацию API и отображались в интерфейсе.

---

## Главная цель

В манифесте задач (`docs/tasks/tasks-manifest.json`) часть задач имела статус `"planned"`. Схема API и интерфейс допускали только статусы `backlog`, `in-progress`, `completed`, `cancelled`. В результате при запросе списка задач возвращалась ошибка 500 «Response doesn't match the schema». Задача — добавить статус `planned` в схему, типы и UI, чтобы такие задачи загружались и отображались без ошибок.

---

## Цели

1. **Бэкенд:** добавить `planned` в enum статусов в схеме Zod и в типах задачи; добавить маппинг статуса «planned» на отображаемую строку в markdown (эмодзи/подпись).
2. **Фронтенд:** добавить `planned` в тип `TaskStatus`, в конфиг бейджа статуса, в фильтры и селекты (список, диалог редактирования).
3. Исключить ошибку сериализации ответа для задач со статусом `"planned"` в манифесте.

---

## Обязательные требования

### Бэкенд (taskViewerBe)

- В **схеме** (`taskViewerBe/src/schemas/tasks.schema.ts`): в `taskStatusEnum` добавить значение `'planned'` (порядок: `backlog`, `planned`, `in-progress`, `completed`, `cancelled`).
- В **типах** (`taskViewerBe/src/types/task.types.ts`): в тип поля `status` добавить `'planned'`.
- В **сервисе** (`taskViewerBe/src/services/tasks.service.ts`): в `statusEmojiMap` для обновления markdown добавить запись `'planned': '📅 Запланировано'`.

### Фронтенд (taskViewerFe)

- В **типе** `TaskStatus` (`taskViewerFe/src/types/task.types.ts`): добавить `'planned'`.
- В **TaskStatusBadge** (`taskViewerFe/src/components/TaskStatusBadge.tsx`): в `statusConfig` добавить `'planned': { label: '📅 Запланировано', variant: 'secondary' }`.
- В **TaskList**, **TaskFilters**, **TaskEditDialog**: в селектах статуса добавить опцию «📅 Запланировано» со значением `planned`.

---

## Реализованные артефакты

### Бэкенд

- `taskViewerBe/src/schemas/tasks.schema.ts` — добавлен `'planned'` в `taskStatusEnum`.
- `taskViewerBe/src/types/task.types.ts` — в тип `status` добавлен `'planned'`.
- `taskViewerBe/src/services/tasks.service.ts` — в `statusEmojiMap` добавлено `'planned': '📅 Запланировано'`.

### Фронтенд

- `taskViewerFe/src/types/task.types.ts` — в `TaskStatus` добавлен `'planned'`.
- `taskViewerFe/src/components/TaskStatusBadge.tsx` — в `statusConfig` добавлен статус `planned`.
- `taskViewerFe/src/components/TaskList.tsx` — в селект статуса добавлен `SelectItem value="planned"`.
- `taskViewerFe/src/components/TaskFilters.tsx` — в фильтр по статусу добавлен `planned`.
- `taskViewerFe/src/components/TaskEditDialog.tsx` — в селект статуса добавлен `planned`.

---

## Критерии приёмки

- [x] В манифесте задачи со статусом `"planned"` не вызывают ошибку сериализации при `GET /api/tasks`.
- [x] Схема и типы бэкенда допускают статус `planned`.
- [x] В UI задачи со статусом «Запланировано» отображаются корректно (бейдж, фильтр, выбор при редактировании).

---

## Вне границ задачи

- Изменение данных манифеста (редактирование существующих задач) не входит в задачу.
- Информативные сообщения об ошибках валидации (TASK-022) были реализованы отдельно.

---

## Связанные артефакты

- Манифест задач: `docs/tasks/tasks-manifest.json` (задачи TASK-017 … TASK-021 со статусом `planned`).
- Бэкенд Task Viewer: `taskViewerBe/`
- Фронтенд Task Viewer: `taskViewerFe/`
