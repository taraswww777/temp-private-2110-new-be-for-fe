# TASK-067: Реализация полноценного WYSIWYG редактора для Markdown

**Статус**: 📋 Backlog  
**Ветка**: `feature/TASK-067-implement-wysiwyg-editor` (при необходимости)  
**Приоритет**: средний  
**Связанные задачи**: TASK-060

---

## Краткое описание

Реализовать полноценный WYSIWYG (What You See Is What You Get) редактор для редактирования Markdown контента задач. Редактор должен позволять пользователям форматировать текст визуальными средствами без необходимости знания синтаксиса Markdown.

---

## Контекст

В рамках задачи TASK-060 была предпринята попытка реализовать WYSIWYG режим для Markdown редактора, но она не увенчалась успехом из-за сложностей с конвертацией между Markdown и HTML, а также проблем с синхронизацией состояния contentEditable элемента.

Текущая реализация использует простой textarea с визуальными кнопками форматирования, которые вставляют Markdown синтаксис. Это требует от пользователей знания Markdown, что не всегда удобно.

---

## Выявленные проблемы и нюансы

### 1. Конвертация Markdown ↔ HTML

**Проблема**: Надежная двусторонняя конвертация между Markdown и HTML является сложной задачей.

**Детали**:
- Markdown имеет множество синтаксических конструкций, которые могут быть представлены в HTML по-разному
- Обратная конвертация HTML → Markdown теряет информацию о форматировании
- Регулярные выражения для парсинга Markdown не покрывают все edge cases
- Вложенные структуры (списки в списках, форматирование внутри списков) сложно обрабатывать

**Примеры проблем**:
- Списки с вложенностью теряют структуру при конвертации
- Комбинации форматирования (жирный + курсив) могут конвертироваться некорректно
- Блоки кода внутри списков или других элементов обрабатываются неправильно
- Пустые строки и пробелы теряются или добавляются лишние

### 2. Синхронизация состояния contentEditable

**Проблема**: Синхронизация между внешним состоянием (Markdown) и внутренним состоянием contentEditable элемента (HTML) работает нестабильно.

**Детали**:
- При изменении `value` извне нужно обновить `innerHTML` contentEditable элемента
- При изменении `innerHTML` через пользовательский ввод нужно конвертировать обратно в Markdown и вызвать `onChange`
- Необходимо сохранять позицию курсора при обновлении извне
- Возможны конфликты между внешними обновлениями и пользовательским вводом

**Примеры проблем**:
- При обновлении `value` извне курсор сбрасывается в начало
- При быстром вводе текста происходят конфликты между `onInput` и `useEffect`
- Пустое содержимое не отображается корректно при первой инициализации
- Состояние `isUpdatingRef` не всегда предотвращает циклические обновления

### 3. Совместимость с React 19

**Проблема**: Популярные библиотеки WYSIWYG редакторов (TipTap, Draft.js и др.) могут быть несовместимы с React 19.

**Детали**:
- TipTap v2.7.0 требует React 17 или 18, не поддерживает React 19
- Альтернативные библиотеки (MDXEditor) также могут иметь проблемы совместимости
- Использование `--legacy-peer-deps` может привести к неожиданным ошибкам в runtime

**Решение**: Необходимо найти библиотеку, совместимую с React 19, или дождаться обновления существующих библиотек.

### 4. Производительность

**Проблема**: Конвертация Markdown ↔ HTML на каждом изменении может быть медленной для больших документов.

**Детали**:
- Регулярные выражения для парсинга могут быть медленными на больших текстах
- Множественные проходы по тексту для разных типов форматирования увеличивают время обработки
- Обновление DOM через `innerHTML` может вызывать перерисовку всего содержимого

### 5. Поддержка всех возможностей Markdown

**Проблема**: Полная поддержка всех возможностей Markdown (таблицы, цитаты, изображения, ссылки с заголовками и т.д.) требует сложной реализации.

**Детали**:
- GFM (GitHub Flavored Markdown) расширения (таблицы, зачеркивание, task lists) требуют дополнительной обработки
- Вставка изображений через WYSIWYG требует управления файлами
- Ссылки с заголовками и tooltips сложно редактировать визуально

---

## Цели

- Реализовать полноценный WYSIWYG редактор для Markdown контента
- Обеспечить надежную двустороннюю конвертацию Markdown ↔ HTML
- Обеспечить стабильную синхронизацию состояния редактора
- Поддержать все основные возможности Markdown (заголовки, списки, ссылки, код, форматирование текста)
- Обеспечить совместимость с React 19
- Обеспечить хорошую производительность даже для больших документов

---

## Критерии приёмки

- [ ] Реализован WYSIWYG редактор, который позволяет форматировать текст визуальными средствами
- [ ] Редактор корректно конвертирует Markdown в HTML для отображения
- [ ] Редактор корректно конвертирует HTML обратно в Markdown при сохранении
- [ ] Состояние редактора синхронизируется с внешним состоянием без потери данных
- [ ] Позиция курсора сохраняется при обновлении содержимого извне
- [ ] Редактор поддерживает все основные возможности Markdown (заголовки, списки, ссылки, код, жирный, курсив)
- [ ] Редактор работает стабильно без конфликтов между пользовательским вводом и внешними обновлениями
- [ ] Редактор совместим с React 19
- [ ] Редактор имеет приемлемую производительность для документов до 10,000 символов

---

## Технические заметки

### Рекомендуемые подходы

1. **Использование готовой библиотеки**:
   - Исследовать библиотеки, совместимые с React 19
   - Рассмотреть использование `@mdxeditor/editor` после проверки совместимости
   - Рассмотреть использование `react-quill` или `slate-react` как альтернативу

2. **Кастомная реализация**:
   - Использовать `contentEditable` с более надежной логикой синхронизации
   - Использовать библиотеку для конвертации Markdown ↔ HTML (например, `marked` или `markdown-it`)
   - Реализовать debounce для конвертации при пользовательском вводе
   - Использовать `MutationObserver` для отслеживания изменений в contentEditable

3. **Гибридный подход**:
   - Использовать готовую библиотеку для конвертации Markdown ↔ HTML
   - Реализовать кастомный UI для визуального редактирования
   - Использовать `document.execCommand` или современный `Selection API` для форматирования

### Важные моменты

- Необходимо тщательно протестировать конвертацию на различных типах Markdown контента
- Необходимо обработать edge cases (пустой контент, только пробелы, специальные символы)
- Необходимо обеспечить доступность (accessibility) редактора
- Необходимо обработать вставку контента из буфера обмена (paste events)
- Необходимо обработать undo/redo функциональность

### Известные ограничения

- Полная поддержка всех возможностей GFM может быть сложной для первой версии
- Вставка изображений может требовать дополнительной реализации для управления файлами
- Таблицы могут быть сложными для визуального редактирования

---

## История изменений

- **2026-02-12**: Создана задача после неудачной попытки реализации WYSIWYG в рамках TASK-060
