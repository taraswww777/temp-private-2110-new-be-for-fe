# TASK-063: Улучшение генератора моков истории статусов — реалистичные последовательности

**Статус**: 📋 Бэклог  
**Ветка**: —

---

## Краткое описание

Доработать генератор моков истории статусов (`genStatusHistoryItemDto.ts`) для создания реалистичных последовательностей статусов вместо случайных. Добавить функцию `genStatusHistorySequence()` с типичными последовательностями статусов (успешный путь, путь с ошибкой, отмена и т.д.) и обеспечить правильную хронологию дат.

**Предыдущая задача:** [TASK-028](TASK-028-add-task-status-history-mocks.md) — добавление моков для эндпоинта истории изменения статусов задачи.

---

## Контекст

В рамках TASK-028 была реализована базовая генерация моков истории статусов, но она создаёт случайные статусы без логических последовательностей. Для более реалистичного тестирования и разработки фронтенда необходимо генерировать типичные последовательности статусов, которые соответствуют реальным жизненным циклам заданий.

---

## Требования

### Frontend (temp-private-2110)

1. **Добавить функцию `genStatusHistorySequence()`** в `temp-private-2110/scripts/generateMockData2/generators/genStatusHistoryItemDto.ts`:
   - Функция должна принимать массив статусов (последовательность) и генерировать историю с правильной хронологией
   - Каждый статус должен иметь правильный `previousStatus` (предыдущий статус в последовательности)
   - Даты должны быть в правильном хронологическом порядке (от старых к новым)
   - Поддержка опциональных полей: `changedBy`, `comment`, `metadata`

2. **Добавить типичные последовательности статусов**:
   - **Успешный путь DAPP -> FC**: `created -> new_dapp -> registered -> upload_generation -> upload_formed -> accepted_dapp -> submitted_dapp -> started -> converting -> completed`
   - **Путь с ошибкой на этапе DAPP**: `created -> new_dapp -> registered -> upload_generation -> failed`
   - **Путь с отменой**: `created -> new_dapp -> registered -> upload_generation -> killed_dapp`
   - **Путь с ошибкой на этапе FC**: `created -> new_dapp -> registered -> upload_formed -> started -> start_failed`
   - **Короткий путь**: `created -> started -> completed`
   - Дополнительные последовательности по необходимости

3. **Обновить `generateMockData2.ts`**:
   - Использовать `genStatusHistorySequence()` вместо простого вызова `genStatusHistoryItemDto()`
   - Выбирать случайную последовательность для каждого задания из набора типичных последовательностей
   - Сохранить возможность использования предопределённых данных через `defaultStatusHistory`

4. **Обеспечить правильную хронологию**:
   - Даты должны быть в правильном порядке (от старых к новым)
   - Интервалы между изменениями статусов должны быть реалистичными (от нескольких минут до нескольких часов/дней)

---

## Критерии приёмки

- [ ] Функция `genStatusHistorySequence()` реализована в `genStatusHistoryItemDto.ts`
- [ ] Добавлены типичные последовательности статусов (успешный путь, ошибки, отмена и т.д.)
- [ ] История имеет правильную хронологию (правильный порядок дат от старых к новым)
- [ ] Каждый статус имеет корректный `previousStatus` (предыдущий статус в последовательности)
- [ ] Обновлён `generateMockData2.ts` для использования `genStatusHistorySequence()`
- [ ] Для каждого задания выбирается случайная последовательность из набора типичных
- [ ] Сохранена поддержка предопределённых данных через `defaultStatusHistory`

---

## Детальные требования к реализации

### 1. Функция genStatusHistorySequence

**Файл**: `temp-private-2110/scripts/generateMockData2/generators/genStatusHistoryItemDto.ts`

```typescript
/**
 * Типичные последовательности статусов для реалистичной генерации истории
 */
const STATUS_SEQUENCES = {
  SUCCESS_DAPP_FC: [
    'created', 'new_dapp', 'registered', 'upload_generation', 
    'upload_formed', 'accepted_dapp', 'submitted_dapp', 
    'started', 'converting', 'completed'
  ],
  ERROR_DAPP: ['created', 'new_dapp', 'registered', 'upload_generation', 'failed'],
  CANCELLED: ['created', 'new_dapp', 'registered', 'upload_generation', 'killed_dapp'],
  ERROR_FC: ['created', 'new_dapp', 'registered', 'upload_formed', 'started', 'start_failed'],
  SHORT_SUCCESS: ['created', 'started', 'completed'],
  // ... дополнительные последовательности
};

/**
 * Генерирует реалистичную последовательность истории статусов на основе массива статусов
 * @param statusSequence - массив статусов в порядке их появления
 * @param startDate - начальная дата (по умолчанию - случайная дата за последние 60 дней)
 * @param options - дополнительные опции (changedBy, comment, metadata)
 * @returns Массив StatusHistoryItemDto с правильной хронологией
 */
export function genStatusHistorySequence(
  statusSequence: string[],
  startDate?: Date,
  options?: {
    changedBy?: string | null;
    comment?: string | null;
    metadata?: Record<string, unknown> | null;
  }
): StatusHistoryItemDto[] {
  // Реализация с правильной хронологией дат
  // Каждый статус должен иметь previousStatus из предыдущего элемента последовательности
}
```

### 2. Обновление generateMockData2.ts

**Файл**: `temp-private-2110/scripts/generateMockData2/generateMockData2.ts`

```typescript
// Выбрать случайную последовательность для задания
const sequences = Object.values(STATUS_SEQUENCES);
const selectedSequence = faker.helpers.arrayElement(sequences);
const history = genStatusHistorySequence(selectedSequence);
statusHistory[task.id] = predefined.statusHistory?.[task.id] ?? history;
```

---

## Связанные задачи и артефакты

- **Предыдущая задача:** [TASK-028](TASK-028-add-task-status-history-mocks.md) — добавление моков для эндпоинта истории изменения статусов задачи
- Генератор моков: `temp-private-2110/scripts/generateMockData2/generators/genStatusHistoryItemDto.ts`
- Главный генератор: `temp-private-2110/scripts/generateMockData2/generateMockData2.ts`
- Статусная модель: `service2110/src/types/status-model.ts`
- Эндпоинт: `GET /api/v1/report-6406/tasks/:id/status-history`

---

## Регистрация

Задание зарегистрировано в `docs/tasks/tasks-manifest.json`.
