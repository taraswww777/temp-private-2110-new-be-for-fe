# TASK-047: Улучшения детализации пакета - Backend

**Статус**: 📅 Запланировано
**Ветка**: `feature/TASK-047-package-details-improvements-backend`

---

## Краткое описание

Добавить поле "Дата последнего успешного копирования" в детализацию пакета, реализовать определение процесса копирования на основе статуса пакета.

---

## Исходное описание задачи

> Данный раздел содержит оригинальное описание задачи для оперативного обращения.

**Источник:** [Замечания от 06.02.2026](https://github.com/taraswww777/temp-private-2110-new-be-for-fe/blob/feature/Masha/docs/Правки%20от%2006.02.md#6406)

### Детализация пакета. Основная информация

Добавить поле "Дата последнего успешного копирования" - будет заполняться, только когда копирование прошло успешно (статус пакета "Выполнено")

При неуспешном копировании дата последнего успешного копирования не будет обновляться, будет только меняться текущий статус (статус пакета "Не выполнено")

### Детализация пакета. Работа с копированием

Нужно на основании статуса пакета, выяснить идёт ли реально процесс копирования и если он всётаки запущен, то нужно отобразить такойже прогрес бар с кнопкой отмена как и на странице ТФР.

---

## Цели

- Добавить поле "Дата последнего успешного копирования" в детализацию пакета
- Реализовать логику определения процесса копирования
- Обеспечить корректное обновление даты при успешном копировании

---

## Детальное описание

### Контекст

В детализации пакета необходимо отображать дату последнего успешного копирования и определять, идёт ли процесс копирования в данный момент.

### Требования

#### 1. Дата последнего успешного копирования

**Поле:** `lastSuccessfulCopyDate` (или аналогичное)

**Логика:**
- Заполняется только когда статус пакета = "Выполнено" (успешное копирование)
- При неуспешном копировании (статус "Не выполнено") дата НЕ обновляется
- Сохраняется предыдущее значение даты успешного копирования

**Endpoint:** `GET /api/v1/report-6406/packages/:id`

**Response:**
```json
{
  "id": "uuid",
  "name": "Название пакета",
  "status": "done",
  "lastSuccessfulCopyDate": "2026-02-05T15:30:00Z",  // Новое поле
  // ... остальные поля
}
```

**Тип:** `string | null` (ISO 8601 date-time, nullable)

#### 2. Определение процесса копирования

**Требование:**
На основании статуса пакета определить, идёт ли процесс копирования.

**Логика:**
- Если статус пакета = "Копирование" (или аналогичный) → процесс копирования активен
- Если статус пакета = "Выполнено" → копирование завершено успешно
- Если статус пакета = "Не выполнено" → копирование завершено с ошибкой

**Поле для Frontend:**
Можно добавить вычисляемое поле `isCopying: boolean` или использовать статус напрямую.

**Альтернатива:**
Добавить поле `canBeCopyToTFR: boolean` в `PacketDto` (как указано в замечаниях).

---

## Критерии приёмки

### Дата последнего успешного копирования
- [ ] Добавлено поле `lastSuccessfulCopyDate` в `PacketDto`
- [ ] Поле заполняется только при успешном копировании (статус "Выполнено")
- [ ] При неуспешном копировании дата не обновляется
- [ ] Поле nullable (может быть null)
- [ ] Обновлена OpenAPI спецификация

### Определение процесса копирования
- [ ] Реализована логика определения активного процесса копирования
- [ ] Добавлено поле `isCopying` или `canBeCopyToTFR` в `PacketDto` (если требуется)
- [ ] Логика основана на статусе пакета
- [ ] Обновлена OpenAPI спецификация

### Общие критерии
- [ ] Все изменения протестированы
- [ ] Логика обновления даты работает корректно
- [ ] Документация обновлена

---

## Порядок выполнения

1. Проанализировать статусную модель пакета
2. Добавить поле `lastSuccessfulCopyDate` в `PacketDto`
3. Реализовать логику обновления даты при изменении статуса
4. Реализовать определение процесса копирования
5. Обновить OpenAPI спецификацию
6. Написать unit-тесты
7. Протестировать все сценарии

---

## Связанные документы

- [Замечания от 06.02.2026](https://github.com/taraswww777/temp-private-2110-new-be-for-fe/blob/feature/Masha/docs/Правки%20от%2006.02.md)
- [Статусы пакета](https://sfera.inno.local/knowledge/pages?id=1799080)

