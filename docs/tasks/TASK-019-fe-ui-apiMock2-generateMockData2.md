# TASK-019: apiMock2 и generateMockData2

**Статус**: 📋 Запланировано  
**Ветка**: _не создана_

**Часть общей работы:** 3 из 3 (второй контракт с бэком service2110). Предыдущая: [TASK-018](TASK-018-fe-ui-apiClient2-fullUpdate2.md).

---

## Краткое описание

Создать пакет **apiMock2**, реализовать **generateMockData2** — генерацию мок-данных для всех эндпоинтов API2 (10–100 элементов на эндпоинт, конфигурируемо) с возможностью предопределённых данных. Добавить apiMock2 в workspaces и скрипт generateMockData2 в package.json.

Документация по скриптам generateMockData2 (запуск, настройка объёма, предопределённые данные), если потребуется, выполняется **отдельной задачей**.

---

## Зависимости

- **TASK-018** выполнена: пакет apiClient2 создан, цепочка api:fullUpdate2 работает.

---

## Исходные данные (согласовано)

| Вопрос | Решение |
|--------|--------|
| generateMockData2 — объём | Генерировать для **всего объёма эндпоинтов** API2. На каждый эндпоинт — от **10 до 100** элементов (в дальнейшем для части эндпоинтов количество будет скорректировано). |
| Предопределённые данные | Нужна возможность добавлять **заранее предопределённые данные** (переопределение для выбранных эндпоинтов/сущностей), по аналогии с текущим generateMockData. |
| Workspaces | Добавить **apiMock2** в workspaces. |

---

## Требования

1. Добавить в **workspaces** в `temp-private-2110/package.json`: **apiMock2**.
2. Создать пакет **apiMock2** (структура под моки второго контракта; место для сгенерированных данных — внутри apiMock2 или отдельная папка в webpackConfigs — на усмотрение реализации).
3. Реализовать **generateMockData2**: обход всех эндпоинтов из спеки/клиента API2, генерация по каждому от 10 до 100 элементов (конфигурируемо), поддержка предопределённых данных (переопределение для выбранных эндпоинтов/сущностей).
4. Добавить в package.json скрипт **generateMockData2**.

---

## Критерии приёмки

- [ ] В workspaces добавлен **apiMock2**.
- [ ] Пакет **apiMock2** создан.
- [ ] Реализован **generateMockData2**: генерация по всем эндпоинтам (10–100 элементов на эндпоинт), поддержка предопределённых данных.
- [ ] В package.json добавлен скрипт **generateMockData2**.

---

## Вне границ данной задачи

- Документация по скриптам generateMockData2 (как запускать, настройка объёма, предопределённые данные) — при необходимости делается **отдельной задачей**.

---

## Связанные задачи и артефакты

- **Предыдущая задача:** TASK-018 (apiClient2, api:fullUpdate2).
- Текущая генерация моков API1: `temp-private-2110/scripts/generateMockData/`, `temp-private-2110/apiMock/`
