# TASK-036: Интеграция Task Viewer Frontend с YouTrack

**Статус**: ✅ Выполнено  
**Ветка**: `feature/TASK-035-youtrack-integration-backend`

---

## Краткое описание

Реализовать UI для интеграции Task Viewer Frontend с YouTrack: создание задач в YouTrack, связывание локальных задач с задачами в YouTrack, управление шаблонами и отображение информации о связях.

---

## Цели

- Добавить UI для создания задач в YouTrack из локальных задач
- Реализовать интерфейс для ручного связывания задач
- Добавить отображение информации о связи с YouTrack в детальном просмотре задачи
- Реализовать UI для управления шаблонами YouTrack
- Обеспечить удобный UX для работы с YouTrack интеграцией

---

## Детальное описание

### Контекст

Это frontend часть интеграции с YouTrack. Backend интеграция реализуется в TASK-035.

### Требования

#### 1. Отображение связи с YouTrack в детальном просмотре задачи

**Местоположение:** Страница детального просмотра задачи (`TaskDetailPage.tsx`)

**Функциональность:**
- Отображение блока с информацией о связи с YouTrack
- Если задача связана (есть массив `youtrackIssueIds`):
  - Отображение списка всех связанных задач YouTrack (readable ID)
  - Для каждой задачи: ссылка на задачу в YouTrack (открывается в новой вкладке)
  - Кнопка "Открыть в YouTrack" для каждой задачи
  - Кнопка "Отвязать" для удаления конкретной связи
  - Кнопка "Связать ещё одну задачу" для добавления дополнительной связи
- Если задача не связана:
  - Кнопка "Создать задачу в YouTrack"
  - Кнопка "Связать с существующей задачей"

**UI Компонент:** `YouTrackLinkCard.tsx`

**Пример отображения (несколько связанных задач):**
```
┌─────────────────────────────────────┐
│ 🔗 YouTrack                         │
├─────────────────────────────────────┤
│ • PROJ-123                          │
│   [Открыть] [Отвязать]             │
│ • PROJ-124                          │
│   [Открыть] [Отвязать]             │
│                                     │
│ [Связать ещё одну задачу]          │
└─────────────────────────────────────┘
```

**Пример отображения (одна связанная задача):**
```
┌─────────────────────────────────────┐
│ 🔗 YouTrack                         │
├─────────────────────────────────────┤
│ Задача: PROJ-123                    │
│ [Открыть в YouTrack] [Отвязать]    │
│ [Связать ещё одну задачу]          │
└─────────────────────────────────────┘
```

**Пример отображения (нет связей):**

```
┌─────────────────────────────────────┐
│ 🔗 YouTrack                         │
├─────────────────────────────────────┤
│ Задача не связана                  │
│ [Создать задачу в YouTrack]        │
│ [Связать с существующей задачей]   │
└─────────────────────────────────────┘
```

#### 2. Создание задачи в YouTrack

**Модальное окно:** `CreateYouTrackIssueDialog.tsx`

**Функциональность:**
- Выбор шаблона для создания задачи
- Предпросмотр шаблона (как будет выглядеть задача)
- Опциональное переопределение кастомных полей
- Отображение прогресса создания
- Обработка ошибок

**Поля формы:**
- Шаблон (выпадающий список)
- Приоритет (опционально, переопределение)
- Исполнитель (опционально, переопределение)
- Другие кастомные поля (если есть в шаблоне)

**После успешного создания:**
- Автоматическое связывание с локальной задачей
- Отображение информации о созданной задаче
- Кнопка "Открыть в YouTrack"

#### 3. Связывание с существующей задачей

**Модальное окно:** `LinkYouTrackIssueDialog.tsx`

**Функциональность:**
- Ввод ID задачи YouTrack (readable ID, например "PROJ-123")
- Валидация существования задачи в YouTrack
- Отображение информации о задаче перед связыванием
- Подтверждение связывания

**Поля формы:**
- ID задачи YouTrack (input с валидацией)
- Предпросмотр задачи (после ввода валидного ID)

#### 4. Управление шаблонами

**Страница:** `YouTrackTemplatesPage.tsx` (новая страница)

**Функциональность:**
- Список всех шаблонов
- Создание нового шаблона
- Редактирование существующего шаблона
- Удаление шаблона
- Предпросмотр шаблона

**Таблица шаблонов:**
- ID шаблона
- Название
- Описание
- Дата создания
- Действия (Редактировать, Удалить, Предпросмотр)

**Форма создания/редактирования:** `TemplateFormDialog.tsx`

**Поля:**
- ID шаблона (readonly при редактировании)
- Название
- Описание
- ID проекта YouTrack
- Шаблон summary (с поддержкой переменных)
- Шаблон description (с поддержкой переменных)
- Кастомные поля (динамическое добавление)

**Поддержка переменных в UI:**
- Подсказки с доступными переменными: `{{taskId}}`, `{{title}}`, `{{content}}`, `{{status}}`, `{{branch}}`
- Предпросмотр с подстановкой реальных значений

#### 5. Навигация

**Добавить в меню:**
- Пункт "Шаблоны YouTrack" в главном меню (если есть)
- Или добавить в настройки/администрирование

**Роутинг:**
- `/youtrack/templates` - страница управления шаблонами
- `/youtrack/queue` - страница очереди операций YouTrack (ссылка в шапке)

### Технические детали

#### API клиент

**Файл:** `src/api/youtrack.api.ts`

**Функции:**
```typescript
export const youtrackApi = {
  // Создание задачи в YouTrack
  async createIssue(taskId: string, templateId?: string, customFields?: Record<string, any>): Promise<YouTrackIssueLink>
  
  // Связывание с существующей задачей (добавляет ID в массив)
  async linkIssue(taskId: string, youtrackIssueId: string): Promise<TaskYouTrackLinks>
  
  // Получение информации о всех связях задачи
  async getIssueLinks(taskId: string): Promise<TaskYouTrackLinks | null>
  
  // Удаление конкретной связи
  async unlinkIssue(taskId: string, youtrackIssueId: string): Promise<void>
  
  // Получение информации о задаче из YouTrack
  async getIssueInfo(youtrackIssueId: string): Promise<YouTrackIssueInfo>
  
  // CRUD для шаблонов
  async getTemplates(): Promise<YouTrackTemplate[]>
  async getTemplate(templateId: string): Promise<YouTrackTemplate>
  async createTemplate(template: CreateYouTrackTemplateInput): Promise<YouTrackTemplate>
  async updateTemplate(templateId: string, template: UpdateYouTrackTemplateInput): Promise<YouTrackTemplate>
  async deleteTemplate(templateId: string): Promise<void>
}
```

#### Типы

**Файл:** `src/types/youtrack.types.ts`

```typescript
export interface YouTrackIssueLink {
  localTaskId: string;
  youtrackIssueId: string;  // Readable ID (например, "PROJ-123")
  youtrackIssueUrl: string;
  youtrackData?: YouTrackIssueInfo;
}

// Информация о всех связях задачи
export interface TaskYouTrackLinks {
  localTaskId: string;
  youtrackIssueIds: string[];  // Массив readable ID задач в YouTrack
  links: YouTrackIssueLink[];  // Детальная информация о каждой связи
}

export interface YouTrackIssueInfo {
  summary: string;
  state?: string;
  priority?: string;
  assignee?: string;
  // другие поля
}

export interface YouTrackTemplate {
  id: string;
  name: string;
  description?: string;
  projectId: string;
  summaryTemplate: string;
  descriptionTemplate: string;
  customFields: Record<string, any>;
}

export interface CreateYouTrackTemplateInput {
  id: string;
  name: string;
  description?: string;
  projectId: string;
  summaryTemplate: string;
  descriptionTemplate: string;
  customFields: Record<string, any>;
}

export type UpdateYouTrackTemplateInput = Partial<CreateYouTrackTemplateInput>;
```

#### Компоненты

1. **YouTrackLinkCard.tsx** - карточка с информацией о связи (пиллы «В очереди» / «Не связана», кнопка «Связать с YT»)
2. **YouTrackConnectDialog.tsx** - единый диалог с вкладками «Создать задачу в YouTrack» (по умолчанию) и «Связать с существующей»; переиспользуется на детальной странице и в таблице задач
3. **YouTrackTemplatesPage.tsx** - страница управления шаблонами
4. **YouTrackQueuePage.tsx** - страница очереди операций YouTrack
5. **PageHeader.tsx** - общая шапка страницы (кнопка «Назад», заголовок, подзаголовок, действия справа)
6. **TemplateFormDialog.tsx** - форма создания/редактирования шаблона
7. **TemplatePreview.tsx** - компонент предпросмотра шаблона

#### Интеграция с существующими компонентами

**TaskDetailPage.tsx:**
- Добавить компонент `YouTrackLinkCard` в правую боковую колонку (CardRightBar) карточки задачи — справа от заголовка и метаданных
- Загружать информацию о связях при загрузке задачи (массив `youtrackIssueIds` из манифеста)
- Обновлять информацию после создания/связывания/отвязывания
- Передавать функцию для обновления связей в компонент

---

## Критерии приёмки

### Отображение связи с YouTrack
- [ ] Компонент `YouTrackLinkCard` отображает информацию о связях
- [ ] Отображается список всех связанных задач YouTrack (если есть несколько)
- [ ] Для каждой связанной задачи отображается ID и ссылка
- [ ] Кнопка "Открыть в YouTrack" открывает задачу в новой вкладке
- [ ] Кнопка "Отвязать" удаляет конкретную связь (принимает ID задачи YouTrack)
- [ ] Кнопка "Связать ещё одну задачу" позволяет добавить дополнительную связь
- [ ] Отображаются кнопки создания/связывания для несвязанных задач
- [ ] Компонент корректно обрабатывает состояние загрузки
- [ ] Компонент корректно отображает случай отсутствия связей

### Создание задачи в YouTrack
- [ ] Диалог `CreateYouTrackIssueDialog` открывается и закрывается корректно
- [ ] Отображается список доступных шаблонов
- [ ] Можно выбрать шаблон для создания
- [ ] Можно переопределить кастомные поля
- [ ] Отображается прогресс создания задачи
- [ ] После успешного создания автоматически создаётся связь
- [ ] Обрабатываются ошибки создания
- [ ] Предпросмотр шаблона работает корректно

### Связывание с существующей задачей
- [ ] Диалог `LinkYouTrackIssueDialog` открывается и закрывается корректно
- [ ] Валидация формата ID задачи YouTrack
- [ ] Проверка существования задачи в YouTrack
- [ ] Проверка, что задача ещё не связана (предотвращение дублирования)
- [ ] Отображение информации о задаче перед связыванием
- [ ] Подтверждение связывания работает корректно
- [ ] После связывания ID добавляется в массив `youtrackIssueIds`
- [ ] Обрабатываются ошибки связывания

### Управление шаблонами
- [ ] Страница `YouTrackTemplatesPage` отображает список шаблонов
- [ ] Можно создать новый шаблон через форму
- [ ] Можно редактировать существующий шаблон
- [ ] Можно удалить шаблон с подтверждением
- [ ] Предпросмотр шаблона показывает результат с подстановкой переменных
- [ ] Подсказки с переменными отображаются в форме
- [ ] Валидация формы создания/редактирования работает

### API интеграция
- [ ] API клиент `youtrack.api.ts` реализован полностью
- [ ] Все функции API клиента работают корректно
- [ ] Обработка ошибок API реализована
- [ ] Типы TypeScript определены корректно

### UI/UX
- [ ] Все компоненты имеют современный и понятный дизайн
- [ ] Сообщения об ошибках понятны пользователю
- [ ] Состояния загрузки отображаются корректно
- [ ] Адаптивность для мобильных устройств (если требуется)
- [ ] Доступность (accessibility) соблюдена

### Интеграция с существующим кодом
- [ ] Компонент `YouTrackLinkCard` интегрирован в `TaskDetailPage`
- [ ] Информация о связи загружается при загрузке задачи
- [ ] Информация обновляется после операций создания/связывания
- [ ] Роутинг для страницы шаблонов настроен

### Тестирование
- [ ] Компоненты можно отобразить без ошибок
- [ ] Формы валидируются корректно
- [ ] API вызовы работают (с моками или реальным backend)
- [ ] Обработка ошибок работает корректно

---

## Порядок выполнения

### Этап 1: Подготовка
1. Создать типы TypeScript для YouTrack
2. Создать API клиент для работы с YouTrack
3. Добавить роутинг для страницы шаблонов

### Этап 2: Компонент отображения связи
1. Создать компонент `YouTrackLinkCard`
2. Интегрировать в `TaskDetailPage`
3. Реализовать загрузку информации о связи
4. Добавить обработку действий (открыть, отвязать)

### Этап 3: Создание задачи в YouTrack
1. Создать диалог `CreateYouTrackIssueDialog`
2. Реализовать выбор шаблона
3. Реализовать форму с кастомными полями
4. Реализовать создание задачи через API
5. Добавить обработку ошибок и успешного создания

### Этап 4: Связывание задачи
1. Создать диалог `LinkYouTrackIssueDialog`
2. Реализовать ввод и валидацию ID задачи
3. Реализовать проверку существования задачи
4. Реализовать связывание через API

### Этап 5: Управление шаблонами
1. Создать страницу `YouTrackTemplatesPage`
2. Реализовать список шаблонов
3. Создать форму `TemplateFormDialog`
4. Реализовать CRUD операции
5. Реализовать предпросмотр шаблонов
6. Добавить подсказки с переменными

### Этап 6: Финальная интеграция и тестирование
1. Проверить все компоненты вместе
2. Протестировать все сценарии использования
3. Исправить найденные ошибки
4. Улучшить UI/UX при необходимости

---

## Уточнения в процессе выполнения

### Навигация и шапка страниц
- Кнопка «Назад» ведёт по истории браузера (`navigate(-1)`), а не на фиксированный URL.
- Кнопка «Назад» всегда располагается слева.
- Введён общий компонент **PageHeader**: кнопка «Назад» слева, опциональные заголовок и подзаголовок по центру, произвольные кнопки (actions) справа. Используется на детальной странице задачи, страницах «Шаблоны YouTrack» и «Очередь YouTrack».

### Детальная страница задачи
- Блок YouTrack размещён в **правой боковой колонке (CardRightBar)** — справа от CardHeader и CardContent, на всю высоту карточки с задачей (заголовок, метаданные, даты). Слева — заголовок, статус, приоритет и метаданные; справа — сайдбар с YouTrack.
- Заголовок «Описание задачи» над блоком описания убран; контент описания отображается без этого заголовка.
- Блок описания прижат к левому краю: у карточки описаня используется отрицательный отступ `-ml-4`, у контента — `pl-0` (без левого паддинга).
- Навигация по заголовкам в Markdown увеличена (ширина панели, размер шрифта, отступы). Контент описания не выходит за пределы по горизонтали (`min-w-0`, `break-words`, прокрутка для `pre`/таблиц).

### Единый диалог связывания с YouTrack
- Вместо двух отдельных диалогов используется один **YouTrackConnectDialog** с двумя вкладками:
  - **«Создать задачу в YouTrack»** (вкладка по умолчанию),
  - **«Связать с существующей»**.
- На детальной странице одна кнопка **«Связать с YT»** открывает этот диалог (по умолчанию вкладка «Создать задачу»). Кнопка **«Связать ещё одну задачу»** открывает тот же диалог с вкладкой **«Связать с существующей»** (проп `initialTab="link"`).
- В таблице задач в колонке YouTrack одна кнопка **«Связать с YT»** открывает тот же диалог с вкладкой по умолчанию (создание). Диалог переиспользуется на списке и на детальной странице.

### Очередь операций YouTrack
- Добавлена отдельная страница **«Очередь YouTrack»** (`YouTrackQueuePage`), роут `/youtrack/queue`. В шапке добавлена ссылка «Очередь YouTrack» рядом с «Задачи» и «Шаблоны YouTrack».
- В сведениях об операции отображается связываемая задача: в ответе API очереди для каждой операции возвращается `data` с полями `taskId`, при необходимости `templateId`, `youtrackIssueId`. В таблице очереди выводится колонка «Задача» со ссылкой на локальную задачу и при необходимости шаблон / ID задачи YouTrack.
- Кнопка «Назад» на странице очереди ведёт по истории и располагается слева (через PageHeader).

### Блокировка действий при нахождении в очереди
- Если для задачи уже есть операция в очереди (создание или связывание), кнопки «Создать задачу в YouTrack» и «Связать с существующей задачей» блокируются (то же для «Связать ещё одну задачу»). При отображении одной кнопки «Связать с YT» она также блокируется при любой операции в очереди по этой задаче.
- В карточке YouTrack при нахождении задания в очереди отображается текст **«Задание в очереди»** (в одну строку с пиллом «Не связана» при отсутствии связей).

### Статусы и пиллы в карточке YouTrack
- Вместо длинных подписей используются компактные **пиллы (Badge) с иконкой** в одну строку:
  - «⏳ В очереди» — когда есть операция в очереди,
  - «○ Не связана» — когда связей нет.
- Оба пилла отображаются в одной строке (flex, gap). Бейдж отображается без переноса текста (`whitespace-nowrap` в компоненте Badge).

### Табличное представление (список задач)
- В колонке YouTrack отображается **один из трёх вариантов** (без дублирования статуса «Не связана»):
  1. **В очереди** — только пилл «⏳ В очереди», без кнопки.
  2. **Есть связь** — ID задачи(задач) YouTrack **ссылкой** на задачу в YouTrack (открытие в новой вкладке). Несколько ID выводятся через пробел/запятую. URL строится из базового URL YouTrack.
  3. **Нет связи** — только кнопка «Связать с YT».
- Статус «Не связана» в таблице не показывается.
- Базовый URL YouTrack для ссылок запрашивается с бэкенда: **GET /api/youtrack/config** возвращает `{ baseUrl: string | null }`. Если `baseUrl` не задан, ID выводятся текстом без ссылки.

### API и бэкенд
- В API клиент добавлены: `getQueueStatus()`, `processQueue()`, `getConfig()` (конфиг YouTrack, в т.ч. baseUrl для ссылок на задачи).
- В ответе GET /api/youtrack/queue для каждой операции в ответ добавлено поле `data` с полями `taskId`, опционально `templateId`, `youtrackIssueId` (для отображения связываемой задачи в UI).

---

## Связанные документы

- TASK-035: Интеграция Task Viewer Backend с YouTrack
- [YouTrack REST API Documentation](https://www.jetbrains.com/help/youtrack/devportal/api-howto-create-issue.html)

---

## Связанные задачи

- **TASK-035**: Интеграция Task Viewer Backend с YouTrack - backend часть интеграции

---


