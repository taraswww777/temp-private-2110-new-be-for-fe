# TASK-053: Обновление документации run-app.md

**Статус**: 📋 Бэклог  
**Ветка**: `feature/TASK-053-update-run-app-documentation`

---

## Краткое описание

Обновить документацию `run-app.md` согласно требованиям из замечаний по ПР: добавить информацию о генерации моков, улучшить структуру и содержание.

---

## Исходное описание задачи

> Данный раздел содержит оригинальное описание задачи для оперативного обращения.

**Источник:** [Замечания по ПР 1928838](file:///d:/Downloads/Telegram%20Desktop/2026-02-06.md)

```
run-app.md должен быть таким [как указано в файле]
```

---

## Цели

- Обновить `run-app.md` согласно требованиям
- Добавить информацию о генерации моков перед первым запуском
- Улучшить структуру и читаемость документации
- Обеспечить полноту информации для разработчиков

---

## Детальное описание

### Контекст

Документация `run-app.md` требует обновления для соответствия текущим требованиям и улучшения UX разработчиков.

### Требования

#### Структура run-app.md

**1. Заголовок:**
```markdown
# Запуск приложения
```

**2. Раздел "Установка зависимостей":**
```markdown
## Установка зависимостей

```bash
npm install
```

Для доступа к внутреннему npm-репозиторию нужен корректный `.npmrc`. См. раздел [Конфигурация npm](#конфигурация-npm) ниже.
```

**3. Раздел "Режим разработки":**
```markdown
### Режим разработки

⚠️ **Внимание:** Перед первым запуском необходимо сгенерировать моки: `npm run mock:generate` (или по отдельности `mock:generate:legacy` и `mock:generate:api2`) иначе приложение не запустится.

```bash
npm run start:mock
```

Приложение на [http://localhost:3001](http://localhost:3001), данные отдаются мок-сервером (axios-mock-adapter). Логи — в консоли браузера.
```

**4. Раздел "Сборка":**
```markdown
## Сборка

```bash
npm run build
```

Последовательно выполняется: генерация моков (`mock:generate`), проверки (TypeScript, ESLint, тесты), production-сборка. Результат — в папке `dist/`.
```

**5. Раздел "Дополнительно":**
```markdown
## Дополнительно

### Конфигурация npm

Для установки пакетов из внутреннего репозитория в корне проекта создайте `.npmrc` по шаблону:

```
@vtb:registry=https://npm.repo-ci.sfera.inno.local/repository/frnt-npm-pub
@openvtb:registry=https://npm.repo-ci.sfera.inno.local/repository/frnt-npm-pub
strict-ssl=false
audit=false
always-auth=true
email=*ваш email*@vtb.ru
_auth=*<base64>*
registry=https://npm.repo-ci.sfera.inno.local/repository/uaod-npm
//sfera.inno.local/:_auth=*<base64>*
//npm.repo-ci.sfera.inno.local/:_auth=*<base64>*
```

Вместо `<base64>` подставьте учётные данные в base64. Пример в консоли браузера: `btoa('VTB*@corp.dev.vtb:password')`.

---

### Анализ размера бандла

```bash
npm run start:analyser
```

---

Запускает dev-сервер с открытием анализатора бандла. Подробнее: [Analyzing Bundle Size](https://facebook.github.io/create-react-app/docs/analyzing-the-bundle-size).

### Стенды

- [ИФТ](https://epa-ig-if-lb.test.vtb.ru/ui/cross/ermo/)
```

---

## Критерии приёмки

- [ ] `run-app.md` обновлён согласно требованиям
- [ ] Добавлена информация о генерации моков перед первым запуском
- [ ] Структура документа соответствует требованиям
- [ ] Все разделы присутствуют и заполнены
- [ ] Все ссылки работают корректно
- [ ] Форматирование markdown корректно
- [ ] Информация актуальна и понятна

---

## Порядок выполнения

1. Проанализировать текущий `run-app.md`
2. Сравнить с требованиями из замечаний
3. Обновить содержимое согласно требованиям
4. Проверить корректность всех ссылок
5. Проверить форматирование markdown
6. Убедиться, что информация полная и понятная

---

## Связанные документы

- [Замечания по ПР 1928838](file:///d:/Downloads/Telegram%20Desktop/2026-02-06.md)
- Текущий `run-app.md`

---

## История изменений статуса

| Дата | Статус | Комментарий |
|------|--------|-------------|
| 2026-02-06 | 📋 Бэклог | Задание создано |
