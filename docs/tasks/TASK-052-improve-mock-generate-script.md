# TASK-052: Улучшение скрипта mock:generate для автоматического вызова при разработке

**Статус**: 📋 Бэклог  
**Ветка**: `feature/TASK-052-improve-mock-generate-script`

---

## Краткое описание

Доработать скрипт `mock:generate` для автоматического вызова при разработке: вызывать всегда при dev разработке, но ничего не делать если данные уже есть.

---

## Исходное описание задачи

> Данный раздел содержит оригинальное описание задачи для оперативного обращения.

**Источник:** [Замечания по ПР 1928838](file:///d:/Downloads/Telegram%20Desktop/2026-02-06.md)

```
Почему бы не доработать скрипт mock:generate и вызывать его всегда при дев разработке, но ничего не делать если данные уже есть?
```

---

## Цели

- Доработать скрипт `mock:generate` для проверки наличия данных
- Автоматически вызывать скрипт при dev разработке
- Пропускать генерацию, если данные уже существуют
- Улучшить UX разработки

---

## Детальное описание

### Контекст

В настоящее время перед первым запуском необходимо вручную выполнять `npm run mock:generate`. Это неудобно и может привести к ошибкам, если разработчик забыл это сделать. Необходимо автоматизировать процесс.

### Требования

#### 1. Улучшение скрипта mock:generate

**Текущее поведение:**
- Скрипт всегда генерирует моки
- Не проверяет наличие существующих данных

**Требуемое поведение:**
- Проверять наличие существующих моковых данных
- Если данные уже есть - пропускать генерацию (или обновлять только при необходимости)
- Если данных нет - генерировать их

**Логика проверки:**
- Проверить существование файлов/папок с моковыми данными
- Проверить актуальность данных (опционально, по дате модификации или версии)
- Если данные актуальны - пропустить генерацию

#### 2. Автоматический вызов при dev разработке

**Интеграция:**
- Добавить вызов `mock:generate` в скрипт `start:mock` или аналогичный
- Вызывать перед запуском dev сервера
- Обеспечить быструю работу (пропуск если данные есть)

**Варианты реализации:**
1. Добавить вызов в `start:mock` скрипт
2. Создать pre-хук для npm скриптов
3. Интегрировать в процесс запуска dev сервера

#### 3. Оптимизация производительности

**Требования:**
- Проверка наличия данных должна быть быстрой
- Генерация должна запускаться только при необходимости
- Логирование должно быть понятным (что происходит и почему)

---

## Критерии приёмки

### Улучшение скрипта mock:generate
- [ ] Скрипт проверяет наличие существующих моковых данных
- [ ] Если данные есть и актуальны - пропускает генерацию
- [ ] Если данных нет - генерирует их
- [ ] Логирование понятно и информативно
- [ ] Скрипт работает быстро при пропуске генерации

### Автоматический вызов
- [ ] Скрипт автоматически вызывается при dev разработке
- [ ] Интеграция не замедляет запуск приложения
- [ ] Работает корректно для всех вариантов запуска (start:mock и т.д.)

### Общие критерии
- [ ] Улучшен UX разработки
- [ ] Нет регрессий в функциональности
- [ ] Документация обновлена (если требуется)

---

## Порядок выполнения

1. Проанализировать текущую реализацию скрипта `mock:generate`
2. Определить структуру моковых данных и способ проверки их наличия
3. Реализовать проверку наличия данных в скрипте
4. Добавить логику пропуска генерации при наличии данных
5. Интегрировать автоматический вызов в dev скрипты
6. Протестировать работу скрипта
7. Обновить документацию (если требуется)

---

## Связанные документы

- [Замечания по ПР 1928838](file:///d:/Downloads/Telegram%20Desktop/2026-02-06.md)
- Скрипты генерации моков: `mock:generate`, `mock:generate:legacy`, `mock:generate:api2`

---

## Технические детали

### Структура моковых данных

Необходимо определить:
- Где хранятся моковые данные (папки, файлы)
- Какие файлы/папки являются индикаторами наличия данных
- Как определить актуальность данных (опционально)

### Варианты реализации проверки

1. **Проверка существования файлов:**
   - Проверить наличие ключевых файлов/папок
   - Если есть - пропустить генерацию

2. **Проверка актуальности:**
   - Сравнить дату модификации исходных файлов (swagger) с датой модификации моков
   - Если моки новее - пропустить генерацию

3. **Флаг/маркер:**
   - Использовать специальный файл-маркер для отслеживания состояния
   - Проверять наличие и содержимое маркера
