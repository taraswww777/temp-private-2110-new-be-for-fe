# TASK-058: Вертикальный скролл в выпадающем списке Select (uiKit)

**Статус**: 📋 Backlog  
**Ветка**: `feature/TASK-058-select-vertical-scroll` (при необходимости)  
**Приоритет**: низкий  
**Теги**: private

---

## Краткое описание

Сделать в компоненте Select (taskViewerFe uiKit) стабильно видимую и работающую вертикальную полосу прокрутки в раскрытом выпадающем списке, когда пунктов больше, чем помещается по высоте.

---

## Контекст

Выпадающий список Select (Radix UI) используется, в частности, для выбора цвета тега на странице «Теги». При большом числе пунктов (16 цветов) список обрезается по высоте (`max-h-96`), но вертикальный скролл либо не появляется, либо остаётся невидимым (в т.ч. при overlay-скроллбарах ОС). В `index.css` добавлены стили для `.select-dropdown-viewport` (WebKit scrollbar, Firefox scrollbar-width/scrollbar-color), тем не менее в текущей конфигурации скролл в моменте победить не удалось.

---

## Цели

- Обеспечить появление вертикальной полосы прокрутки в выпадающем списке Select при переполнении контента.
- Сделать полосу прокрутки заметной и удобной (достаточная ширина, контраст в светлой и тёмной теме).
- Сохранить совместимость с Radix UI Select и текущим использованием компонента в приложении.

---

## Критерии приёмки

- [ ] При открытии Select с числом пунктов, превышающим высоту списка, отображается вертикальная полоса прокрутки.
- [ ] Скроллбар виден в типичных браузерах (Chrome, Firefox, Safari, Edge) и в светлой/тёмной теме.
- [ ] Прокрутка списка колёсиком и перетаскиванием ползунка работает корректно.
- [ ] Решение не ломает существующие места использования Select (списки цветов, статусы, приоритеты и т.д.).

---

## Технические заметки

- Компонент: `taskViewerFe/src/uiKit/select.tsx` (SelectContent, SelectPrimitive.Viewport).
- Стили скролла: `taskViewerFe/src/index.css` (класс `.select-dropdown-viewport`).
- Возможные направления: проверка применения стилей в Portal, явная высота viewport, альтернатива Radix (например, кастомный dropdown) при необходимости.
