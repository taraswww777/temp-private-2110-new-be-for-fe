# TASK-033: Улучшение навигации в React Router — решение проблемы с относительными путями

**Статус**: 📋 Бэклог  
**Ветка**: `feature/TASK-033-react-router-navigation-improvement`

---

## Краткое описание

Решить проблему с навигацией в React Router 5, при которой всегда нужно указывать полный путь. Текущее решение с использованием `useRouteMatch` не позволяет переходить между роутами на одном уровне или на уровень выше. Рассмотреть возможность миграции на React Router 6 или улучшить текущее решение.

---

## Контекст

В проекте используется React Router 5.3.4 (`react-router-dom`). При работе с вложенными роутами возникает проблема: для навигации всегда нужно указывать полный путь, что усложняет код и делает его менее поддерживаемым.

**Примеры текущего использования:**
```tsx
// Всегда нужно указывать полный путь
<Link to={'/report-6406/packages'} ... />
history.push('/report-6406/packages');

// Использование useRouteMatch для получения базового пути
const { url } = useRouteMatch(); // например '/report-6406'
<Link to={`${url}/packages`} ... /> // '/report-6406/packages'
```

**Проблемы текущего подхода:**
1. `useRouteMatch` позволяет получить только текущий URL, но не позволяет:
   - Перейти на роут того же уровня (`/report-6406/packages` → `/report-6406/tfr`)
   - Перейти на уровень выше (`/report-6406/packages` → `/report-6406`)
   - Использовать относительные пути (`../tfr`, `./tfr`)

2. Текущее решение требует создания методов генерации полного URL для каждого роута:
   ```tsx
   url: (basePath: string) => `${basePath}/packages`
   ```
   И подкидывать туда `basePath` из стора, что усложняет архитектуру.

**Связанный коммит:** `e93ee2506f57a375b278c71088077c5fa247e4d7` — хорошо раскрывает кейс проблемы.

---

## Требования

### Анализ проблемы

1. **Изучить текущую реализацию навигации:**
   - Проанализировать использование `useRouteMatch` в компонентах
   - Выявить все места, где требуется указание полного пути
   - Оценить сложность текущего решения

2. **Изучить возможности React Router 6:**
   - Изучить новые возможности навигации в React Router 6
   - Проверить поддержку относительных путей
   - Оценить обратную совместимость и сложность миграции

### Варианты решения

#### Вариант 1: Улучшение текущего решения (React Router 5)

1. **Создать утилиты для генерации URL:**
   - Создать модуль с функциями генерации URL для каждого роута
   - Использовать контекст или хук для получения базового пути модуля
   - Обеспечить возможность навигации на одном уровне и выше

2. **Создать кастомный хук для навигации:**
   - Хук, который знает структуру роутов модуля
   - Поддержка относительных путей (`../`, `./`)
   - Поддержка навигации на одном уровне

3. **Использовать контекст для базового пути:**
   - Создать контекст, который хранит базовый путь модуля
   - Использовать его в компонентах для генерации относительных путей

#### Вариант 2: Миграция на React Router 6 (рекомендуется)

1. **Обновить зависимости:**
   - Обновить `react-router-dom` с версии 5.3.4 до версии 6.x
   - Обновить `@types/react-router-dom` до соответствующей версии

2. **Мигрировать компоненты:**
   - Заменить `useHistory` на `useNavigate`
   - Заменить `useRouteMatch` на `useMatch` или `useLocation`
   - Обновить `<Switch>` на `<Routes>`
   - Обновить `<Route>` компоненты на новый синтаксис

3. **Использовать относительные пути:**
   - Использовать относительные пути в `<Link>` и `navigate()`
   - Использовать `relative` prop для указания направления навигации

4. **Обновить роутинг модулей:**
   - Обновить `Report6406Module.routing.tsx`
   - Обновить `ReportTemplateModule.routing.tsx`
   - Обновить другие модули роутинга

---

## Критерии приёмки

### Для варианта 1 (улучшение текущего решения)

- [ ] Создан модуль с утилитами для генерации URL
- [ ] Создан кастомный хук для навигации с поддержкой относительных путей
- [ ] Обновлены все компоненты для использования новых утилит/хуков
- [ ] Реализована возможность навигации на одном уровне (`/report-6406/packages` → `/report-6406/tfr`)
- [ ] Реализована возможность навигации на уровень выше (`/report-6406/packages` → `/report-6406`)
- [ ] Все существующие переходы работают корректно
- [ ] Код протестирован и не содержит регрессий

### Для варианта 2 (миграция на React Router 6)

- [ ] Обновлены зависимости (`react-router-dom` до версии 6.x)
- [ ] Мигрированы все компоненты с использованием `useHistory` на `useNavigate`
- [ ] Мигрированы все компоненты с использованием `useRouteMatch` на новые хуки
- [ ] Обновлены все модули роутинга (`<Switch>` → `<Routes>`, новый синтаксис `<Route>`)
- [ ] Реализована поддержка относительных путей в навигации
- [ ] Все существующие переходы работают корректно
- [ ] Код протестирован и не содержит регрессий
- [ ] Обновлена документация по использованию роутинга

---

## Область изменений

### Для варианта 1 (улучшение текущего решения)

| Файл | Описание |
|------|----------|
| `temp-private-2110/src/utils/routing.ts` | Новый модуль с утилитами для генерации URL |
| `temp-private-2110/src/hooks/useNavigation.ts` | Новый хук для навигации с поддержкой относительных путей |
| `temp-private-2110/src/contexts/RoutingContext.tsx` | Контекст для базового пути модуля (опционально) |
| Компоненты с навигацией | Обновление использования навигации |

### Для варианта 2 (миграция на React Router 6)

| Файл | Описание |
|------|----------|
| `temp-private-2110/package.json` | Обновление зависимостей |
| `temp-private-2110/src/modules/Report6406Module/Report6406Module.routing.tsx` | Миграция на новый синтаксис |
| `temp-private-2110/src/modules/ReportTemplateModule/ReportTemplateModule.routing.tsx` | Миграция на новый синтаксис |
| `temp-private-2110/src/modules/ReportsMenuModule/ReportsMenuModule.routing.tsx` | Миграция на новый синтаксис |
| Все компоненты с `useHistory` | Замена на `useNavigate` |
| Все компоненты с `useRouteMatch` | Замена на новые хуки |
| Все компоненты с `<Link>` | Обновление для использования относительных путей |

---

## Вне границ данной задачи

- Изменение структуры роутов
- Добавление новых роутов
- Изменение логики авторизации/аутентификации
- Изменение логики работы с историей браузера

---

## Дополнительные заметки

### Преимущества React Router 6

1. **Относительные пути:**
   - Поддержка относительных путей в `<Link>` и `navigate()`
   - Возможность указать направление навигации через `relative` prop

2. **Улучшенный API:**
   - `useNavigate` вместо `useHistory`
   - Более простой и интуитивный API
   - Лучшая типизация

3. **Улучшенная производительность:**
   - Оптимизированный рендеринг
   - Лучшая поддержка Suspense

### Примеры использования в React Router 6

```tsx
// Относительные пути
<Link to="../tfr">TFR</Link>
<Link to="./packages">Packages</Link>
navigate("../tfr");
navigate("./packages");

// Указание направления
<Link to="../tfr" relative="path">TFR</Link>
<Link to="../tfr" relative="route">TFR</Link>
```

### Риски миграции

1. **Breaking changes:**
   - Изменения в API могут потребовать обновления большого количества компонентов
   - Необходимость тестирования всех переходов

2. **Зависимости:**
   - Проверка совместимости с другими библиотеками
   - Возможные проблемы с типами TypeScript

### Рекомендация

**Рекомендуется вариант 2 (миграция на React Router 6)** по следующим причинам:
- Нативное решение проблемы с относительными путями
- Улучшенный API и производительность
- Долгосрочная поддержка (React Router 5 в maintenance mode)
- Лучшая типизация и DX (Developer Experience)

Однако перед принятием решения необходимо:
1. Оценить объем изменений
2. Проверить совместимость с другими библиотеками
3. Создать план миграции по этапам
4. Протестировать на отдельной ветке

---

## Связанные задачи и артефакты

- React Router документация: https://reactrouter.com/
- React Router 6 migration guide: https://reactrouter.com/en/main/upgrading/v5
- Коммит с примером проблемы: `e93ee2506f57a375b278c71088077c5fa247e4d7`
- Текущая версия: `react-router-dom@5.3.4`
