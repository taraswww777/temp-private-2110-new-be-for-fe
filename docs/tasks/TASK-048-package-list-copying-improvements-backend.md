# TASK-048: Улучшения списка пакетов - работа с копированием (Backend)

**Статус**: 📋 Бэклог  
**Ветка**: `feature/TASK-048-package-list-copying-improvements-backend`

---

## Краткое описание

Реализовать функционал отмены копирования пакетов, добавить сортировку по размеру, реализовать блокировку копирования при активном процессе, добавить проверку уникальности имени пакета на ТФР.

---

## Исходное описание задачи

> Данный раздел содержит оригинальное описание задачи для оперативного обращения.

**Источник:** [Замечания от 06.02.2026](https://github.com/taraswww777/temp-private-2110-new-be-for-fe/blob/feature/Masha/docs/Правки%20от%2006.02.md#6406)

### Список пакетов

Добавить кнопку "Отменить копирование" для каждой записи.

Отображать кнопку только если пакет находится в статусе активного копирования.

Добавить сортировку по размеру.

Если запущен процесс копирования пакетов (статус пакета "Копирование") нельзя копировать другие пакеты - заблокировать кнопку "Копировать в ТФР".

По хорошему нужен эндпоинт по которому можно определить запущен ли процесс копирования.
или в `PackageDto` добавить атрибут `canBeCopyToTFR:boolean` - вычисляется на основе статусной модели.

При выборе пакетов и нажатии кнопки "Копировать на ТФР" проверять(на бэке), нет ли пакета с таким наименованием уже на ТФР? Если есть, то ошибка на UI

---

## Цели

- Реализовать endpoint отмены копирования пакета
- Добавить поддержку сортировки по размеру
- Реализовать проверку активного процесса копирования
- Добавить проверку уникальности имени пакета на ТФР

---

## Детальное описание

### Контекст

В списке пакетов необходимо реализовать функционал работы с копированием: отмена, блокировка при активном процессе, проверка уникальности имени.

### Требования

#### 1. Отмена копирования пакета

**Endpoint:** `POST /api/v1/report-6406/packages/:id/cancel-copy`

**Функциональность:**
- Отменить процесс копирования для указанного пакета
- Изменить статус пакета на соответствующий (например, "Отменено" или "Не выполнено")
- Остановить процесс копирования (если возможно)

**Response:**
```json
{
  "id": "uuid",
  "status": "cancelled",
  "message": "Копирование отменено"
}
```

**Валидация:**
- Пакет должен быть в статусе "Копирование"
- Если статус другой - вернуть ошибку

#### 2. Сортировка по размеру

**Endpoint:** `POST /api/v1/report-6406/packages/list` (или GET с query параметрами)

**Требование:**
- Добавить возможность сортировки по размеру пакета
- Поддержать сортировку по возрастанию и убыванию

**Параметры:**
```json
{
  "sorting": {
    "column": "size",
    "direction": "asc" // или "desc"
  }
}
```

#### 3. Проверка активного процесса копирования

**Вариант 1: Endpoint для проверки**
**Endpoint:** `GET /api/v1/report-6406/packages/copy-status`

**Response:**
```json
{
  "isCopying": true,
  "activePackets": ["uuid1", "uuid2"]
}
```

**Вариант 2: Поле в PacketDto**
Добавить поле `canBeCopyToTFR: boolean` в `PacketDto`, которое вычисляется на основе статусной модели.

**Логика:**
- `canBeCopyToTFR = true` если нет активных процессов копирования
- `canBeCopyToTFR = false` если есть хотя бы один пакет в статусе "Копирование"

**Рекомендация:** Использовать Вариант 2 (поле в DTO) для упрощения.

#### 4. Проверка уникальности имени пакета на ТФР

**Endpoint:** `POST /api/v1/report-6406/packages/copy-to-tfr`

**Валидация:**
- Перед началом копирования проверить, существует ли пакет с таким именем на ТФР
- Если существует - вернуть ошибку 400

**Request:**
```json
{
  "packetIds": ["uuid1", "uuid2"]
}
```

**Response при ошибке:**
```json
{
  "error": "PACKET_NAME_EXISTS_ON_TFR",
  "message": "Пакет с таким наименованием уже существует на ТФР",
  "details": {
    "packetName": "Название пакета",
    "packetId": "uuid"
  }
}
```

---

## Критерии приёмки

### Отмена копирования
- [ ] Реализован endpoint `POST /api/v1/report-6406/packages/:id/cancel-copy`
- [ ] Endpoint отменяет процесс копирования
- [ ] Статус пакета обновляется корректно
- [ ] Валидация статуса работает
- [ ] Обновлена OpenAPI спецификация
- [ ] Добавлены unit-тесты

### Сортировка по размеру
- [ ] Добавлена поддержка сортировки по размеру
- [ ] Сортировка работает по возрастанию и убыванию
- [ ] Обновлена OpenAPI спецификация

### Проверка активного процесса
- [ ] Реализована проверка активного процесса копирования
- [ ] Добавлено поле `canBeCopyToTFR` в `PacketDto` (или endpoint)
- [ ] Логика основана на статусной модели
- [ ] Обновлена OpenAPI спецификация

### Проверка уникальности на ТФР
- [ ] Реализована проверка уникальности имени пакета на ТФР
- [ ] При дубликате возвращается ошибка 400
- [ ] Ошибка содержит информацию о существующем пакете
- [ ] Обновлена OpenAPI спецификация
- [ ] Добавлены unit-тесты

### Общие критерии
- [ ] Все изменения протестированы
- [ ] Обработка ошибок работает корректно
- [ ] Документация обновлена

---

## Порядок выполнения

1. Проанализировать статусную модель пакета
2. Реализовать endpoint отмены копирования
3. Добавить сортировку по размеру
4. Реализовать проверку активного процесса копирования
5. Реализовать проверку уникальности имени на ТФР
6. Обновить OpenAPI спецификацию
7. Написать unit-тесты
8. Протестировать все сценарии

---

## Связанные документы

- [Замечания от 06.02.2026](https://github.com/taraswww777/temp-private-2110-new-be-for-fe/blob/feature/Masha/docs/Правки%20от%2006.02.md)
- [Статусы пакета](https://sfera.inno.local/knowledge/pages?id=1799080)

---

## История изменений статуса

| Дата | Статус | Комментарий |
|------|--------|-------------|
| 2026-02-06 | 📋 Бэклог | Задание создано |
