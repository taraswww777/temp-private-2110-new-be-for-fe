# TASK-054: Добавление приоритетов задач в манифест и интерфейс

**Статус**: ✅ Выполнено  
**Ветка**: `feature/TASK-054-add-task-priorities`  
**Дата завершения**: 2026-02-06

---

## Краткое описание

Добавить поддержку приоритетов задач в манифесте (по умолчанию medium), реализовать фильтрацию по приоритету и статусу, добавить сортировку по приоритету в интерфейсе Task Viewer.

---

## Цели

- Добавить поле `priority` в структуру задач манифеста
- Установить значение по умолчанию `medium` для всех существующих и новых задач
- Реализовать фильтрацию по приоритету и статусу в интерфейсе
- Добавить сортировку по приоритету
- Обновить Backend и Frontend для поддержки приоритетов

---

## Детальное описание

### Контекст

В настоящее время задачи в манифесте не имеют приоритетов. Необходимо добавить возможность приоритизации задач для более эффективного управления бэклогом и планирования работ.

### Требования

#### 1. Структура данных

**Приоритеты:**
- `low` - Низкий приоритет
- `medium` - Средний приоритет (по умолчанию)
- `high` - Высокий приоритет
- `critical` - Критический приоритет

**Обновление манифеста:**
```json
{
  "id": "TASK-001",
  "title": "Название задачи",
  "status": "backlog",
  "priority": "medium",  // Новое поле
  "file": "TASK-001-file.md",
  "createdDate": null,
  "completedDate": null,
  "branch": null
}
```

**Миграция существующих данных:**
- Все существующие задачи должны получить `priority: "medium"`
- Обновить все записи в `tasks-manifest.json`

#### 2. Backend изменения (taskViewerBe)

**Обновление типов:**
```typescript
export interface Task {
  id: string;
  title: string;
  status: 'backlog' | 'in-progress' | 'completed' | 'cancelled';
  priority: 'low' | 'medium' | 'high' | 'critical';  // Новое поле
  file: string;
  createdDate: string | null;
  completedDate: string | null;
  branch: string | null;
}
```

**Обновление схем Zod:**
```typescript
export const taskPriorityEnum = z.enum(['low', 'medium', 'high', 'critical']);

export const taskSchema = z.object({
  id: z.string(),
  title: z.string(),
  status: taskStatusEnum,
  priority: taskPriorityEnum.default('medium'),  // По умолчанию medium
  file: z.string(),
  createdDate: z.string().nullable(),
  completedDate: z.string().nullable(),
  branch: z.string().nullable(),
});
```

**Обновление API:**
- `GET /api/tasks` - возвращает приоритеты
- `GET /api/tasks/:id` - возвращает приоритет
- `PATCH /api/tasks/:id` - позволяет обновлять приоритет

**Параметры фильтрации и сортировки:**
- Добавить поддержку фильтрации по приоритету в query параметрах (если требуется)
- Добавить поддержку сортировки по приоритету

#### 3. Frontend изменения (taskViewerFe)

**Обновление типов:**
```typescript
export type TaskPriority = 'low' | 'medium' | 'high' | 'critical';

export interface Task {
  id: string;
  title: string;
  status: TaskStatus;
  priority: TaskPriority;  // Новое поле
  file: string;
  createdDate: string | null;
  completedDate: string | null;
  branch: string | null;
}
```

**Компонент фильтрации:**
- Добавить фильтр по приоритету в `TaskFilters.tsx`
- Поддержка множественного выбора приоритетов
- Комбинированная фильтрация по статусу и приоритету

**Компонент сортировки:**
- Добавить сортировку по приоритету в `TaskList.tsx`
- Порядок приоритетов: `critical` > `high` > `medium` > `low`
- Поддержка сортировки по возрастанию и убыванию

**Отображение приоритета:**
- Добавить визуальное отображение приоритета в списке задач
- Использовать Badge или иконки для обозначения приоритета
- Цветовая индикация (например: critical - красный, high - оранжевый, medium - синий, low - серый)

**Редактирование приоритета:**
- Добавить возможность изменения приоритета в `TaskEditDialog.tsx`
- Выпадающий список с приоритетами

**Пример UI фильтра:**
```
┌─────────────────────────────────────┐
│ Фильтры:                            │
│                                     │
│ Статус: [Все ▼]                    │
│ Приоритет: [Все ▼]                 │
│                                     │
│ [Все] [Low] [Medium] [High] [Crit] │
└─────────────────────────────────────┘
```

**Пример отображения в таблице:**
```
ID      | Название        | Статус    | Приоритет | ...
TASK-001| Задача 1        | Бэклог    | 🔴 High   | ...
TASK-002| Задача 2        | В работе  | 🟡 Medium | ...
TASK-003| Задача 3        | Бэклог    | 🟢 Low    | ...
```

#### 4. Миграция данных

**Скрипт миграции:**
- Создать скрипт для добавления `priority: "medium"` ко всем существующим задачам
- Обновить `tasks-manifest.json`
- Обеспечить обратную совместимость

---

## Критерии приёмки

### Backend (taskViewerBe)
- [ ] Добавлено поле `priority` в тип `Task`
- [ ] Обновлена Zod схема с поддержкой приоритетов
- [ ] Значение по умолчанию `medium` установлено
- [ ] API endpoints возвращают приоритеты
- [ ] API позволяет обновлять приоритет через PATCH
- [ ] Все существующие задачи имеют приоритет `medium`
- [ ] Обновлена OpenAPI спецификация (если используется)

### Frontend (taskViewerFe)
- [ ] Добавлен тип `TaskPriority`
- [ ] Обновлены типы `Task` и `TaskDetail`
- [ ] Реализована фильтрация по приоритету
- [ ] Реализована комбинированная фильтрация по статусу и приоритету
- [ ] Реализована сортировка по приоритету
- [ ] Приоритет отображается в списке задач
- [ ] Приоритет можно редактировать в диалоге редактирования
- [ ] Визуальное отображение приоритета (Badge/иконки)
- [ ] Цветовая индикация приоритетов

### Миграция данных
- [ ] Создан скрипт миграции
- [ ] Все существующие задачи обновлены с `priority: "medium"`
- [ ] `tasks-manifest.json` обновлён корректно
- [ ] Обратная совместимость обеспечена

### Общие критерии
- [ ] Все изменения протестированы
- [ ] UI/UX интуитивен и понятен
- [ ] Нет регрессий в функциональности
- [ ] Документация обновлена (если требуется)

---

## Порядок выполнения

### Этап 1: Backend изменения
1. Обновить типы TypeScript в `taskViewerBe`
2. Обновить Zod схемы с поддержкой приоритетов
3. Обновить API endpoints
4. Создать скрипт миграции данных
5. Выполнить миграцию существующих задач
6. Протестировать Backend изменения

### Этап 2: Frontend изменения
1. Обновить типы TypeScript в `taskViewerFe`
2. Обновить API клиент
3. Добавить фильтрацию по приоритету в `TaskFilters`
4. Добавить сортировку по приоритету в `TaskList`
5. Добавить отображение приоритета в таблице
6. Добавить редактирование приоритета в `TaskEditDialog`
7. Добавить визуальную индикацию приоритетов
8. Протестировать Frontend изменения

### Этап 3: Интеграция и тестирование
1. Протестировать полный цикл работы с приоритетами
2. Проверить фильтрацию и сортировку
3. Проверить миграцию данных
4. Убедиться в отсутствии регрессий

---

## Технические детали

### Порядок приоритетов для сортировки

```typescript
const priorityOrder: Record<TaskPriority, number> = {
  critical: 4,
  high: 3,
  medium: 2,
  low: 1
};

// Сортировка по убыванию (высокий приоритет сначала)
tasks.sort((a, b) => priorityOrder[b.priority] - priorityOrder[a.priority]);
```

### Цветовая схема приоритетов

```typescript
const priorityColors: Record<TaskPriority, string> = {
  critical: 'destructive',  // Красный
  high: 'default',         // Оранжевый/Жёлтый
  medium: 'secondary',     // Синий
  low: 'outline'           // Серый
};
```

### Пример фильтрации

```typescript
const filteredTasks = tasks.filter(task => {
  const statusMatch = statusFilter === 'all' || task.status === statusFilter;
  const priorityMatch = priorityFilter.length === 0 || priorityFilter.includes(task.priority);
  return statusMatch && priorityMatch;
});
```

---

## Связанные документы

- TASK-004: Создание Task Viewer приложения
- `docs/tasks/tasks-manifest.json`
- `taskViewerBe/src/types/task.types.ts`
- `taskViewerFe/src/types/task.types.ts`

---

## История изменений статуса

| Дата | Статус | Комментарий |
|------|--------|-------------|
| 2026-02-06 | 📋 Бэклог | Задание создано |
| 2026-02-06 | ✅ Выполнено | Задание выполнено со всеми уточнениями |

---

## Уточнения и изменения в процессе реализации

### 1. Фильтр по приоритету
- **Изменение**: Фильтр по приоритету реализован как выпадающий список Select (аналогично фильтру по статусу), а не как кнопки с множественным выбором
- **Причина**: Единообразие интерфейса с фильтром по статусу

### 2. Изменение приоритетов в таблице
- **Изменение**: Приоритет можно изменять прямо в таблице через Select (аналогично изменению статуса)
- **Причина**: Удобство и быстрота редактирования без открытия диалога

### 3. Блокировка приоритета для выполненных задач
- **Изменение**: Для задач со статусом `completed` изменение приоритета заблокировано (disabled)
- **Причина**: Выполненные задачи не должны менять приоритет

### 4. Детальная страница задачи
- **Добавлено**: Отображение и редактирование статуса и приоритета на детальной странице
- **Расположение**: Статус и приоритет отображаются в CardHeader рядом с ID задачи
- **Функциональность**: Оба поля можно редактировать через Select прямо на странице

### 5. Постраничная навигация
- **Добавлено**: Постраничная навигация для главной страницы со списком задач
- **Функции**: 
  - Выбор количества элементов на странице (10, 25, 50, 100)
  - Навигация между страницами с кнопками первой/последней/предыдущей/следующей
  - Отображение информации "Показано X–Y из Z"
  - Автоматический сброс на первую страницу при изменении фильтров

### 6. UI улучшения
- **Ширина колонки ID**: Увеличена до 6rem (класс `w-24` в Tailwind)
- **Вертикальные паддинги**: Уменьшены в два раза:
  - Header: `py-4` → `py-2`
  - Main: `py-8` → `py-4`
- **Заголовок страницы**: Заголовок "Задачи проекта" и "Всего задач: X" в одну строку
- **Фильтры**: Все фильтры в одну строку с `flex-grow` для равномерного распределения пространства

---

## Важные замечания

**Важно:** При миграции данных необходимо убедиться, что все существующие задачи получат приоритет `medium`, чтобы не нарушить работу системы.
