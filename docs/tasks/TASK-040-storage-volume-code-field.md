# TASK-040: Добавление поля code в DTO хранилища и отображение ТФР только на странице ТФР

**Статус**: ✅ Выполнено (Backend)  
**Ветка**: `feature/TASK-040-storage-volume-code-field`

---

## Краткое описание

Добавить поле `code` в DTO хранилища (`/api/v1/report-6406/storage/volume`), экспортировать enum значений в Swagger. Обеспечить отображение ТФР только на странице ТФР.

---

## Исходное описание задачи

> Данный раздел содержит оригинальное описание задачи для оперативного обращения.

**Источник:** [Заметки от 05.02.2026](../temp-private-2110/docs/notes/2026-02-05.md#отображение-размеров-в-футере-тарас)

```
ТФР нужно отображать только на странице ТФР, и только его.

С дто приходящем по запросу из  `/api/v1/report-6406/storage/volume`, добавить поле `code`.

Варианты значений этого кода должны быть в enum, и экспортироваться в свагер.
```

---

## Цели

- Добавить поле `code` в DTO хранилища
- Создать enum для кодов хранилищ
- Экспортировать enum в Swagger спецификацию
- Обеспечить отображение ТФР только на соответствующей странице

---

## Детальное описание

### Контекст

В настоящее время в футере отображаются размеры всех хранилищ. Необходимо отображать только ТФР на странице ТФР. Для этого нужно добавить поле `code` в DTO, чтобы Frontend мог фильтровать хранилища.

### Требования

#### 1. Добавление поля code в DTO

**Endpoint:** `GET /api/v1/report-6406/storage/volume`

**Текущая структура Response (до изменений):**
```json
[
  {
    "id": "default",
    "name": "Корзина 1",
    "totalHuman": "1.00 TB",
    "freeHuman": "535.72 GB",
    "percent": 47.5
  }
]
```

**Новая структура Response (после изменений):**
```json
[
  {
    "id": "default",
    "name": "Корзина 1",
    "code": "LOCAL",  // Новое обязательное поле
    "totalHuman": "1.00 TB",
    "freeHuman": "535.72 GB",
    "percent": 47.5
  }
]
```

#### 2. Enum для кодов хранилищ

**Создать enum (Zod схема):**
```typescript
export const storageCodeSchema = z.enum(['TFR', 'S3', 'LOCAL']);
export type StorageCode = z.infer<typeof storageCodeSchema>;
```

**Значения enum:**
- `'TFR'` — хранилище ТФР
- `'S3'` — S3 хранилище
- `'LOCAL'` — локальное хранилище

**Требования:**
- Enum должен быть экспортирован в Swagger спецификацию как `StorageCodeEnumSchema`
- Значения должны быть понятными и соответствовать реальным хранилищам
- Enum регистрируется в `openapi-components.ts` и `schema-registry.ts`

#### 3. Отображение ТФР только на странице ТФР

**Требование Frontend:**
- На странице ТФР отображать только хранилище с `code: "TFR"`
- На других страницах не отображать ТФР в футере

**Примечание:** Это требование для Frontend, но Backend должен предоставить необходимые данные.

---

## Выявленные уточнения при реализации

### 1. Конкретные значения enum

Enum `StorageCode` содержит три значения:
- `'TFR'` — для хранилища ТФР
- `'S3'` — для S3 хранилища
- `'LOCAL'` — для локального хранилища (например, "Корзина 1")

### 2. Маппинг хранилищ на коды

В генераторе моков установлено соответствие между названиями хранилищ и кодами:
- `"Корзина 1"` → `'LOCAL'`
- `"Корзина 2"` → `'S3'`
- `"ТФР"` → `'TFR'`

### 3. Текущее хранилище в Backend

В текущей реализации Backend возвращает одно хранилище "Корзина 1" с кодом `'LOCAL'`:
```typescript
const DEFAULT_STORAGE_NAME = 'Корзина 1';
const DEFAULT_STORAGE_CODE: StorageCode = 'LOCAL';
```

### 4. Регистрация enum в Swagger

Enum зарегистрирован в OpenAPI компонентах как `StorageCodeEnumSchema` и доступен для использования в Swagger документации.

### 5. Обязательность поля

Поле `code` является обязательным в схеме `storageVolumeItemSchema` и должно присутствовать во всех ответах endpoint `/api/v1/report-6406/storage/volume`.

### 6. Структура Response (актуальная)

**Актуальная структура Response:**
```json
[
  {
    "id": "default",
    "name": "Корзина 1",
    "code": "LOCAL",
    "totalHuman": "1.00 TB",
    "freeHuman": "535.72 GB",
    "percent": 47.5
  }
]
```

---

## Критерии приёмки

### Backend
- [x] Добавлено поле `code` в DTO хранилища
- [x] Создан enum `StorageCode` с корректными значениями (`'TFR'`, `'S3'`, `'LOCAL'`)
- [x] Enum экспортирован в Swagger спецификацию (`StorageCodeEnumSchema`)
- [x] Endpoint `/api/v1/report-6406/storage/volume` возвращает поле `code`
- [x] Обновлена OpenAPI спецификация
- [ ] Добавлены unit-тесты (опционально)

### Frontend (вынесено в отдельную задачу TASK-065)
- [ ] Обновлены типы для поддержки поля `code` (требуется регенерация API клиента)
- [ ] Реализована фильтрация хранилищ по коду на странице ТФР (используется фильтрация по `name`)
- [ ] ТФР отображается только на странице ТФР (частично: фильтрация работает, но по `name`)

**Примечание:** Работы на стороне Frontend вынесены в отдельную задачу [TASK-065](TASK-065-frontend-storage-volume-code-field.md).

### Общие критерии
- [x] Все изменения протестированы (Backend)
- [x] Документация обновлена (OpenAPI спецификация)

---

## Порядок выполнения

1. Проанализировать текущую структуру DTO хранилища
2. Создать enum `StorageCode`
3. Добавить поле `code` в DTO
4. Экспортировать enum в Swagger
5. Обновить генератор моков (если используется)
6. Обновить OpenAPI спецификацию
7. Написать unit-тесты
8. Обновить Frontend (если требуется)
9. Протестировать изменения

---

## Связанные документы

- [Заметки от 05.02.2026](../temp-private-2110/docs/notes/2026-02-05.md)

---

## Выполненные изменения

### Backend (✅ Завершено)

#### 1. Добавление поля code в DTO
- **Файл:** `service2110/src/schemas/report-6406/storage.schema.ts`
- Добавлено поле `code: storageCodeSchema` в схему `storageVolumeItemSchema`
- Поле является обязательным

#### 2. Создание enum StorageCode
- **Файл:** `service2110/src/schemas/report-6406/storage.schema.ts`
- Создан enum `storageCodeSchema` с значениями: `['TFR', 'S3', 'LOCAL']`
- Тип `StorageCode` экспортирован

#### 3. Экспорт enum в Swagger
- **Файлы:**
  - `service2110/src/schemas/openapi-components.ts`
  - `service2110/src/schemas/schema-registry.ts`
- Enum зарегистрирован как `StorageCodeEnumSchema` в OpenAPI компонентах
- Доступен в Swagger спецификации

#### 4. Обновление сервиса
- **Файл:** `service2110/src/services/report-6406/storage.service.ts`
- Метод `getStorageVolume()` возвращает поле `code` со значением `'LOCAL'` для хранилища "Корзина 1"

#### 5. Обновление OpenAPI спецификации
- Swagger спецификация обновлена автоматически
- Поле `code` присутствует в `StorageVolumeItemDto` с ссылкой на `StorageCodeEnumSchema`

### Frontend (📋 Отложено в TASK-065)

Работы на стороне Frontend вынесены в отдельную задачу [TASK-065](TASK-065-frontend-storage-volume-code-field.md):
- Регенерация API клиента
- Обновление фильтрации на всех страницах для использования поля `code` вместо `name`

