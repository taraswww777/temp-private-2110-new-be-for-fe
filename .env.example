# =============================================================================
# AI Tasks Runner — переменные окружения
# Скрипт загружает переменные из корневого .env репозитория (файл .env в корне).
# Скопируйте этот файл в корень репо как .env.example и раскомментируйте нужные строки.
# Документация: scripts/ai-tasks-runner/README.md
# =============================================================================

# Путь к JSON-конфигу (относительно корня репо). Если не задан — используется config.default.json
# AI_TASKS_CONFIG=scripts/ai-tasks-runner/config.json

# Путь к манифесту заданий (относительно корня репо)
# AI_TASKS_MANIFEST_PATH=docs/tasks/tasks-manifest.json

# Папка с документами задач (относительно корня репо)
# AI_TASKS_TASKS_DIR=docs/tasks

# Папка для логов агентов (создаётся при необходимости)
# AI_TASKS_LOGS_DIR=docs/logs

# Базовая ветка, от которой создаются ветки для заданий
# AI_TASKS_BASE_BRANCH=main

# Шаблон имени ветки: {{id}} — id задачи, {{slug}} — slug из имени файла
# AI_TASKS_BRANCH_TEMPLATE=feature/{{id}}-{{slug}}

# Максимум попыток выполнения на одно задание (число)
# AI_TASKS_MAX_ATTEMPTS=5

# Фильтр по статусу в манифесте: обрабатывать только задачи с этим статусом (например planned)
# AI_TASKS_STATUS_FILTER=planned

# Коммитить ли файлы логов из docs/logs в репозиторий (true / false / 1 / 0 / yes)
# AI_TASKS_COMMIT_LOGS=true

# Делать ли пустой коммит, если после шага агента нет изменений (true / false / 1 / 0 / yes)
# AI_TASKS_EMPTY_COMMIT_IF_NO_CHANGES=true

# Имя remote для push
# AI_TASKS_REMOTE=origin

# -----------------------------------------------------------------------------
# Команды агентов (переопределяют execAgent/verifyAgent/actualizeAgent из конфига).
# Формат: одна строка, первый токен — команда, остальное — аргументы.
# В аргументах можно использовать плейсхолдеры; путь к задаче и т.д. передаются через env
# (TASK_PATH, ATTEMPT, MANIFEST_PATH, BRANCH_NAME).
# -----------------------------------------------------------------------------

# Агент выполнения задания (по ТЗ — агент Cursor). Exit 0 = успех.
# AI_TASKS_EXEC_CMD=node scripts/ai-tasks-runner/agents/exec-stub.js

# Агент проверки результата. Exit 0 = задание решено верно.
# AI_TASKS_VERIFY_CMD=node scripts/ai-tasks-runner/agents/verify-stub.js

# Агент актуализации (обновление документа задачи и манифеста после успешной проверки).
# AI_TASKS_ACTUALIZE_CMD=node scripts/ai-tasks-runner/agents/actualize-stub.js
