# Utility Scripts

## kill-port.js

Утилита для освобождения занятых портов в Windows.

### Использование

```bash
# Через npm scripts
npm run kill:3000   # Освободить порт 3000
npm run kill:3001   # Освободить порт 3001
npm run kill:5173   # Освободить порт 5173
npm run kill:all    # Освободить все порты

# Напрямую
node scripts/kill-port.js 3000
node scripts/kill-port.js <любой_порт>
```

### Как это работает

1. Ищет процессы, использующие указанный порт
2. Получает PID (Process ID) каждого процесса
3. Завершает процессы принудительно
4. Выводит информативные сообщения

### Примеры вывода

**Порт свободен:**
```
🔍 Searching for processes on port 3000...
✅ Port 3000 is free
```

**Процесс найден и убит:**
```
🔍 Searching for processes on port 3000...
⚠️  Found 1 process(es) using port 3000
✅ Killed process PID 12345
✨ Port 3000 is now free
```

## Автоматическое освобождение портов

Некоторые npm скрипты автоматически освобождают порты перед запуском:

- `npm run dev` → автоматически освобождает порт 3000
- `npm run taskViewer:dev` → автоматически освобождает порты 3001 и 5173

Это достигается через pre-hooks: `predev` и `pretaskViewer:dev`
