# Utility Scripts

## generate-swagger-html.js

Генератор HTML документации из Swagger спецификации.

### Использование

```bash
# Через npm scripts (рекомендуется)
npm run docs:generate   # Сгенерировать HTML документацию
npm run docs:preview    # Сгенерировать и показать путь для просмотра

# Напрямую
node scripts/generate-swagger-html.js
```

### Что делает скрипт

1. Читает `service2110/docs/swagger/swagger.json`
2. Создаёт папку `docs/public/`
3. Копирует `swagger.json` в `docs/public/`
4. Генерирует `index.html` со встроенным Swagger UI
5. Выводит пути к сгенерированным файлам

### Примеры вывода

```
✅ Copied swagger.json to docs/public/
✅ Generated index.html in docs/public/

📄 Documentation files:
   - D:\ws\work\temp-private-2110-new-be-for-fe\docs\public\index.html
   - D:\ws\work\temp-private-2110-new-be-for-fe\docs\public\swagger.json

🌐 Open index.html in browser to preview.
```

### Особенности HTML

- Использует Swagger UI 5.11.0 из CDN
- Топбар скрыт (более чистый интерфейс)
- TryItOut отключен (только просмотр)
- Deep linking включен
- Фильтр endpoints включен
- Тема подсветки синтаксиса: monokai

### Когда использовать

- **Локальное тестирование**: Проверить как будет выглядеть документация на GitHub Pages
- **Офлайн просмотр**: Просмотр документации без запуска сервера
- **Отправка клиентам**: Можно отправить HTML файл для просмотра документации

### Примечания

- Папка `docs/public/` находится в `.gitignore`
- Скрипт использует ES modules (import/export)
- Swagger UI загружается из CDN (требуется интернет для просмотра HTML)

## kill-port.js

Утилита для освобождения занятых портов в Windows.

### Использование

```bash
# Через npm scripts
npm run kill:3000   # Освободить порт 3000
npm run kill:3001   # Освободить порт 3001
npm run kill:5173   # Освободить порт 5173
npm run kill:all    # Освободить все порты

# Напрямую
node scripts/kill-port.js 3000
node scripts/kill-port.js <любой_порт>
```

### Как это работает

1. Ищет процессы, использующие указанный порт
2. Получает PID (Process ID) каждого процесса
3. Завершает процессы принудительно
4. Выводит информативные сообщения

### Примеры вывода

**Порт свободен:**
```
🔍 Searching for processes on port 3000...
✅ Port 3000 is free
```

**Процесс найден и убит:**
```
🔍 Searching for processes on port 3000...
⚠️  Found 1 process(es) using port 3000
✅ Killed process PID 12345
✨ Port 3000 is now free
```

## Автоматическое освобождение портов

Некоторые npm скрипты автоматически освобождают порты перед запуском:

- `npm run dev` → автоматически освобождает порт 3000
- `npm run taskViewer:dev` → автоматически освобождает порты 3001 и 5173

Это достигается через pre-hooks: `predev` и `pretaskViewer:dev`
