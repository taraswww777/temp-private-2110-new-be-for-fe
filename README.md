# Temp Private 2110 - New BE for FE

Монорепозиторий с Backend проектом на стеке Fastify + TypeScript + PostgreSQL + Drizzle ORM.

## Структура проекта

```
/
├── be/                 # Backend приложение (Fastify + TypeScript)
├── docs/               # Документация и задачи
│   └── tasks/         # Задачи в формате TASK-XXX
├── package.json       # Корневой package.json с workspace
└── README.md          # Этот файл
```

## Требования

- Node.js >= 20
- Docker и docker-compose
- npm

## Быстрый старт

### 1. Установка зависимостей

```bash
npm install
```

### 2. Запуск в режиме разработки (рекомендуется)

**Вариант A: БД в Docker + Backend локально** ⭐ (лучше для разработки)

```bash
# Запустить PostgreSQL в Docker
npm run docker:db:up

# Запустить Backend локально с hot-reload
npm run be:dev
```

**Преимущества:**
- ✅ Быстрый hot-reload при изменении кода
- ✅ Удобная отладка
- ✅ Логи сразу в терминале

**Вариант B: Всё в Docker** (production-like)

```bash
# Запустить PostgreSQL + Backend в Docker
npm run docker:up
```

**Преимущества:**
- ✅ Полная изоляция
- ✅ Ближе к production окружению
- ✅ Не нужно устанавливать Node.js локально

### 3. Проверка работы

После запуска приложение будет доступно:

- **API**: http://localhost:3000
- **Swagger UI**: http://localhost:3000/docs
- **Health Check**: http://localhost:3000/health

## Доступные команды

### Backend команды

```bash
# Разработка
npm run be:dev              # Запуск в dev режиме с hot-reload
npm run be:build            # Сборка TypeScript
npm run be:lint             # Проверка кода ESLint
npm run be:lint:fix         # Автоисправление ESLint

# База данных
npm run be:db:generate      # Создать миграцию
npm run be:db:migrate       # Применить миграции
npm run be:db:push          # Быстрое применение изменений (dev)
npm run be:db:studio        # Открыть Drizzle Studio
```

### Docker команды

```bash
# PostgreSQL
npm run docker:db:up        # Запустить только PostgreSQL
npm run docker:db:down      # Остановить PostgreSQL

# Полное приложение
npm run docker:up           # Запустить PostgreSQL + Backend
npm run docker:down         # Остановить всё
npm run docker:logs         # Показать логи контейнеров
```

## Варианты запуска (подробно)

### 🔧 Режим разработки (Development)

**Рекомендуемый способ для разработки:**

1. Запустить PostgreSQL:
   ```bash
   npm run docker:db:up
   ```

2. Запустить Backend локально:
   ```bash
   npm run be:dev
   ```

3. При необходимости применить миграции:
   ```bash
   npm run be:db:push
   ```

**Что происходит:**
- PostgreSQL работает в Docker контейнере `be_postgres`
- Backend запускается локально через `tsx watch` с автоперезагрузкой
- При изменении `.ts` файлов сервер автоматически перезапускается
- БД доступна на `localhost:5432`
- API доступен на `localhost:3000`

**Остановка:**
```bash
# Остановить Backend: Ctrl+C в терминале
# Остановить PostgreSQL:
npm run docker:db:down
```

---

### 🐳 Docker режим (Production-like)

**Полная изоляция в контейнерах:**

1. Запустить всё:
   ```bash
   npm run docker:up
   ```

**Что происходит:**
- PostgreSQL работает в контейнере `be_postgres`
- Backend работает в контейнере `be_app` (собирается из `Dockerfile`)
- Оба контейнера в одной Docker сети
- API доступен на `localhost:3000`

**Просмотр логов:**
```bash
npm run docker:logs
```

**Остановка:**
```bash
npm run docker:down
```

---

## API Endpoints

### Health Check
- `GET /health` - Проверка состояния приложения и БД

### Items (примеры CRUD)
- `GET /api/v1/items` - Получить все элементы
- `GET /api/v1/items/:id` - Получить элемент по ID
- `POST /api/v1/items` - Создать элемент
- `PATCH /api/v1/items/:id` - Обновить элемент
- `DELETE /api/v1/items/:id` - Удалить элемент

### Документация
- `GET /docs` - Swagger UI (интерактивная документация)
- `GET /docs/json` - Swagger JSON спецификация

## Работа с базой данных

### Создание и применение миграций

1. Изменить схему в `be/src/db/schema/`
2. Создать миграцию:
   ```bash
   npm run be:db:generate
   ```
3. Применить миграцию:
   ```bash
   npm run be:db:migrate
   ```

### Быстрое применение изменений (dev)

Для разработки можно использовать `db:push` (без создания миграций):

```bash
npm run be:db:push
```

### Drizzle Studio

Визуальный редактор базы данных:

```bash
npm run be:db:studio
```

Откроется по адресу: https://local.drizzle.studio

## Переменные окружения

Backend использует файл `be/.env`. Пример в `be/.env.example`:

```env
# Application
NODE_ENV=development
PORT=3000
HOST=0.0.0.0

# Database
DB_HOST=localhost        # для локального запуска
# DB_HOST=postgres       # для Docker режима
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=app_db
```

**Важно:** При локальном запуске Backend используйте `DB_HOST=localhost`, при Docker режиме - `DB_HOST=postgres`.

## Структура Backend

```
be/
├── src/
│   ├── config/         # Конфигурация (env валидация)
│   ├── db/            # База данных
│   │   ├── schema/    # Drizzle схемы таблиц
│   │   ├── migrations/# SQL миграции
│   │   └── index.ts   # Подключение к БД
│   ├── routes/        # API маршруты
│   ├── services/      # Бизнес-логика
│   ├── schemas/       # Zod схемы валидации
│   ├── plugins/       # Fastify плагины
│   ├── app.ts         # Настройка Fastify
│   └── server.ts      # Entry point
├── docs/
│   └── swagger/       # Swagger документация (генерируется)
├── Dockerfile         # Docker образ для Backend
├── docker-compose.yml # Оркестрация контейнеров
├── tsconfig.json      # TypeScript конфигурация
├── eslint.config.mjs  # ESLint конфигурация
├── drizzle.config.ts  # Drizzle ORM конфигурация
└── README.md          # Backend документация
```

## Технологический стек

- **Runtime**: Node.js 20
- **Framework**: Fastify
- **Language**: TypeScript
- **Validation**: Zod
- **Database**: PostgreSQL 16
- **ORM**: Drizzle ORM
- **API Docs**: Swagger (автогенерация из Zod)
- **Linter**: ESLint
- **Containerization**: Docker, docker-compose

## Особенности

- ✅ TypeScript с строгой типизацией
- ✅ Валидация через Zod с автогенерацией типов
- ✅ Swagger документация с автогенерацией из Zod схем
- ✅ CORS настроен для localhost
- ✅ Graceful shutdown
- ✅ Health check endpoint с проверкой БД
- ✅ Версионирование API (v1)
- ✅ RFC 7807 Problem Details для ошибок
- ✅ Логирование запросов/ответов в dev режиме (pino-pretty)
- ✅ Hot-reload в dev режиме (tsx watch)
- ✅ Docker и docker-compose для запуска

## Разработка

### Добавление нового endpoint

1. Создать Zod схему в `be/src/schemas/`
2. Создать сервис в `be/src/services/`
3. Создать маршрут в `be/src/routes/`
4. Зарегистрировать маршрут в `be/src/routes/index.ts`
5. Swagger документация обновится автоматически

### Добавление новой таблицы

1. Создать схему в `be/src/db/schema/`
2. Экспортировать из `be/src/db/schema/index.ts`
3. Создать миграцию: `npm run be:db:generate`
4. Применить миграцию: `npm run be:db:migrate`

## Troubleshooting

### Порт 3000 занят

```bash
# Windows
netstat -ano | findstr :3000
taskkill //F //PID <PID>

# Linux/Mac
lsof -i :3000
kill -9 <PID>
```

### PostgreSQL не запускается

```bash
# Остановить все контейнеры
npm run docker:down

# Удалить volumes (осторожно, удалит данные!)
cd be && docker-compose down -v

# Запустить заново
npm run docker:db:up
```

### Health check возвращает 503

Убедитесь, что PostgreSQL запущен:
```bash
npm run docker:db:up
```

Проверьте, что в `be/.env` правильный `DB_HOST`:
- Для локального запуска: `DB_HOST=localhost`
- Для Docker режима: `DB_HOST=postgres`

## Задачи

Все задачи находятся в папке `docs/tasks/` в формате `TASK-XXX-описание.md`.

Манифест задач: `docs/tasks/tasks-manifest.json`

## Лицензия

ISC
